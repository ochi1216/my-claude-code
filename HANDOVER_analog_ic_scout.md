# 引継ぎ資料: analog_ic_scout（仮称）構想

**作成日**: 2026-07-15
**引継ぎ元**: Claude Code（`rtocs_organizer` 開発セッション）
**引継ぎ先**: Claude Code（別セッション）
**対象者**: 越智さん（アナログ半導体のシニアシステムエンジニア）

---

## 1. 背景・依頼の経緯

このセッションでは主に `rtocs_organizer/`（大前研一RTOCS講義の分析ツール群）を開発していた。その完成後、越智さんから「アナログ半導体のシニアシステムエンジニア」という立場で、同じ考え方をTexas Instruments(TI)のアナログ製品分析に応用できないかという相談があった。

越智さんの原案（5ステップ）:
1. TIのWebサイトからアナログ製品を1つ抽出するインターフェースツール
2. その製品のアプリケーションと市場（需要予測・CAGR）を、TIのアプリケーションノートを起点に調査するツール
3. 調査した市場のキーカスタマー（TI製品を採用する可能性が高い主要顧客）を調査・リストアップするツール
4. 米国・欧州・アジア（日本・中国）の主要アナログ半導体メーカーの競合ICを調査し仕様比較するツール
5. 上記の分析を踏まえ、TI製品ベースで次に開発すべきデバイスのKPI・追加機能を提案するツール

越智さんからは「シニアシステムエンジニアとして、もっとこうすべきという機能・流れの一元管理アイデアをまとめてほしい」との依頼があり、Claudeが `rtocs_organizer` で確立したアーキテクチャパターンを踏まえた統合案を提示。Q&Aで主要な設計方針を確定させたが、**このセッションでは実装に着手していない（構想・設計のみ）**。実装は別セッションで行う方針。

---

## 2. 目指すゴール

「型番を1つ入力したら、TIサイト情報の取り込み→市場分析→キーカスタマー推定→競合IC比較→次世代スペック提案までを一気通貫でまとめる」ツール。将来的には分析済み製品を蓄積し、ポートフォリオ横断で「どの製品ファミリーが競合に押されているか」等の傾向を俯瞰できるダッシュボードに発展させる。

`rtocs_organizer` で学んだ最大の教訓は「単発の一気通貫パイプラインだけでは傾向が掴めない」ことだったため、今回も**同じ2フェーズ構成**を最初から組み込むことを提案している:
- **(A) ポートフォリオ俯瞰ダッシュボード** — 蓄積した分析済み製品を横断して見る
- **(B) 単一製品ディープダイブ・パイプライン** — 越智さん原案の5ステップ

---

## 3. 確定済みの方針（Q&Aで確定・2026-07-15）

| 論点 | 決定内容 |
|---|---|
| 競合IC仕様比較のデータ取得 | Octopart/Nexar等の有料APIは**使わない**。無料のLLM検索グラウンディングのみで組む |
| キーカスタマー調査の扱い | 非公開の顧客情報調査は非現実的・非倫理的なため、**公開情報のみからの推定**に限定（特許引用・公開テカルダウンレポート・TI公表事例）。常に「推定であり確定情報ではない」と明記する |
| 進行段階 | **構想・設計段階**。実装はまだ着手していない |

---

## 4. 具体化した設計

### フォルダ・ファイル構成案（`rtocs_organizer` と同じ命名規則を踏襲）

```
analog_ic_scout/                       # 仮称。正式名称は着手時に確定
  ic_index.py                  # 製品ケースライブラリ（analyzed_products.json）の増分構築。rtocs_index.pyと同型
  ic_prompts.py                 # 各ステージのLLMプロンプト定数
  ic_engine.py                   # 5ステージパイプライン本体（Streamlit非依存）
  ic_report.py                   # HTMLレポート生成（rtocs_organizerのカードCSSを流用）
  ic_dashboard_YYYYMMDD_01.py   # Streamlit本体（3タブ）
  requirements.txt
  CHANGELOG.md
  README.md
```

### 中核となる設計思想

1. **製品ケースライブラリ＋インデックス**（`rtocs_organizer/rtocs_index.py` と同型）を中核に据える。1製品＝1構造化JSON。蓄積するほどポートフォリオ横断の傾向分析ができるようになる
2. **事実には必ず出典・確度タグを持たせる**: `{value, source: "TI"/"third_party"/"llm_estimate"/"推定", confidence}`。この業界情報最大のリスクは「TIのポジショントーク」と「客観的事実」の混同であり、`rtocs_organizer` の単純な「(要確認)」注記よりも一段厳格な設計が必要
3. **製品カテゴリ別のパラメトリックスキーマ**: オペアンプとDC-DCコンバータでは比較すべきKPIが全く異なるため、一律固定スキーマにせず「カテゴリ→比較パラメータ一覧」の設定を持たせる。例:
   - オペアンプ: 供給電圧範囲, 消費電流(quiescent), GBW, オフセット電圧, ノイズ, パッケージ, 温度グレード, AEC-Q100
   - DC-DCコンバータ: 入力/出力電圧範囲, 効率(%), スイッチング周波数, 静止電流, パッケージ, 温度グレード
4. **各ステージは失敗しても続行、部分レポート生成**（`rtocs_organizer` の `strategy_engine.py` と同じ失敗耐性方針を踏襲）

### 5ステージ設計表

| # | ステージ | 手段 | 出力 |
|---|---|---|---|
| 0 | 製品取り込み | TIサイトHTML取得＋データシートPDFをPyMuPDFで構造化パース（カテゴリ別スキーマに従う） | `{part_number, category, key_specs:{...}, applications:[...], datasheet_url}` |
| 1 | 市場分析 | LLM検索グラウンディングで、named applicationsごとに第三者機関（Yole/TechInsights/Omdia/S&P Global等）のCAGR・市場規模を検索。TI自社発信の数値も別途保持し出典タグで区別 | `[{application, market_estimate:{value, source, confidence}, growth_driver}]` |
| 2 | キーカスタマー推定 | LLM検索グラウンディングで特許引用（Google Patents等）・公開テカルダウン記事（TechInsights/System Plus Consulting等）・TI公表事例を検索 | `[{company, evidence_type, evidence_summary, confidence:"推定"}]` |
| 3 | 競合IC比較 | 主要競合（米: ADI/onsemi/Microchip、欧: Infineon/STMicro、亜: ROHM/Renesas(日)、中国メーカー等、地域タグ付き）をLLM検索グラウンディングで1社ずつ検索し、カテゴリ別スキーマに沿った比較可能な近似品番・スペックを取得 | `[{competitor, region, comparable_part, specs:{...同スキーマ}, gap_vs_ti}]` |
| 4 | 次世代スペック提案 | 全ステージ統合LLM合成（`rtocs_organizer` の「戦略策定」ステージと同型） | 優先順位付きスペック差分リスト（根拠・実現性リスク・目標KPI付き） |

### ダッシュボード構成案（3タブ、`rtocs_organizer` と同じパターン）

1. 📦 **製品登録・検索** — 型番入力→ステージ0実行、既存ケースライブラリの検索
2. 📊 **ポートフォリオ俯瞰** — 蓄積した分析済み製品を横断し、カテゴリ別のCAGR分布、競合ギャップの多い製品ランキング等を可視化
3. 🎯 **製品ディープダイブ** — 型番入力→ステージ0〜4を一気通貫実行→HTMLレポート生成

---

## 5. 未決事項（次セッションで詰めるべき点）

1. **主要競合メーカーのデフォルトリスト**: 米欧亜・中国を含め、標準で何社を対象にするか未確定
2. **TIサイトの取得方式**: 単純HTTP取得（requests+BeautifulSoup）で足りるか、JS依存でSelenium必須かは、実際のTI製品ページ・アプリケーションノートページの構造を見て判断する必要がある（未調査）
3. **フォルダ名**: `analog_ic_scout` は仮称。正式名称は着手時に越智さんと確定する

---

## 6. 再利用すべき既存資産（`rtocs_organizer/`）

このセッションで作った以下のファイルは、そのまま設計・実装パターンとして流用できる:

| ファイル | 流用できる部分 |
|---|---|
| `rtocs_organizer/rtocs_index.py` | ケースライブラリの増分構築ロジック（mtimeベースの差分検出、compact_index_for_llm形式） |
| `rtocs_organizer/strategy_engine.py` | パイプラインの失敗耐性設計（`_run_stage`による`{"error":...}`格納＋続行）、コスト集計（トークン→USD→円換算）、**LLM検索グラウンディングの実装**（`google-genai`の`Client`＋`types.Tool(google_search=types.GoogleSearch())`。レガシーの`google-generativeai`は`google_search_retrieval`しか公開しておらずGemini 2.x系では400エラーになる、という重要な教訓を含む） |
| `rtocs_organizer/strategy_prompts.py` | プロンプト設計パターン（システムプレフィックス、JSON出力スキーマの書き方、検索グラウンディング用プロンプトの書き方） |
| `rtocs_organizer/strategy_report.py` | HTMLレポートのカード型CSS、失敗/スキップ時のフォールバック表示、matplotlibチャートのbase64埋め込み |
| `rtocs_organizer/rtocs_dashboard_20260715_04.py` | Streamlit 3タブ構成の実装パターン（`st.status`によるステージ進捗表示、`st.cache_data`によるインデックスキャッシュ） |
| `rtocs_organizer/CHANGELOG.md`, `README.md` | バージョン管理ルール（`ツール名_yyyymmdd_連番.py`、旧版を残したまま新版追加、ツールフォルダごとにCHANGELOG.md）の実例 |

---

## 7. 運用ルール（リポジトリ全体で適用中）

ルート `README.md` の「開発ルール（バージョン管理）」節を参照。要点:
- バージョンアップ時のファイル名は `ツール名_yyyymmdd_連番.py`
- 旧バージョンは削除・上書きせず新版と併存させる（リポジトリから旧版も直接ダウンロード可能な状態を保つ）
- 各ツールフォルダに `CHANGELOG.md` を置く

`analog_ic_scout` を新設する際も、このルールに従うこと。

---

## 8. 次セッションでの作業開始チェックリスト

1. 本ドキュメントと `rtocs_organizer/strategy_engine.py`（特にgoogle-genaiでのグラウンディング実装）を読み、既存パターンを把握する
2. 未決事項①②（競合メーカーのデフォルトリスト、TIサイトの取得方式）を越智さんと確認する
3. `rtocs_organizer` と同様、Explore/Planサブエージェントによる詳細設計フェーズに進む（フォルダ名・カテゴリ別スキーマの初期セットを含む）
4. 実装時は本リポジトリの運用ルール（バージョン命名・CHANGELOG）に従うこと

---

## 9. コミュニケーション上の注意

- 呼称は「越智さん」
- 回答は日本語・結論先出し
- 越智さんはプログラム初心者を自認しつつ、専門領域（半導体）については高度な要求を出してくる。技術的な実現可能性（スクレイピングの法的リスク、API有無、データの信頼性）を率直に指摘し、楽観的な機能一覧の提示だけで終わらせないこと
- 「まだ構想段階」という越智さんの明言を尊重し、次セッションでも実装を急がず、未決事項の確認から入ること
