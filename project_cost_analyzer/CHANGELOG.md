# CHANGELOG — project_cost_analyzer

このフォルダ内の変更履歴。バージョンアップ時は旧ファイルを残したまま新ファイルを追加し、ここに変更点を追記する。

## [20260722_01] - 2026-07-22

**追加ファイル:** `project_cost_analyzer_20260722_01.py`, `requirements.txt`, `README.md`, `run_dashboard.bat`

初版。Project CostのKOB1シート（SAPプロジェクトコスト実績明細）を読み込み、プロジェクト単位で
コスト分析を行うStreamlitダッシュボードを新規作成。

- Supabase等の外部サービスへデータをアップロードする方式を検討したが、社内の機密性の高い
  コストデータであることと、環境の自動判定（安全分類器）が外部への一括データ送信をブロック
  したことから、ローカル環境で完結する構成（Excel直接読み込み→ローカル集計→Streamlit表示）
  に方針転換した
- Project Name / Fiscal Year / Project status / Cost Category での絞り込み、プロジェクト別・
  期間別・Cost Category別・Cost Element別・Function別の集計、明細テーブル表示、Excel出力に対応
- Windows用起動バッチ`run_dashboard.bat`を追加。フォルダ内の`project_cost_analyzer_*.py`を
  ファイル名降順（`dir /o-n`）で判定し、最新リビジョンを自動的に起動する（バージョンアップ時に
  バッチファイル自体の書き換えは不要）

## [20260722_02] - 2026-07-22

**追加ファイル:** `project_cost_analyzer_20260722_02.py`（`20260722_01`からのコピー＋機能追加。旧版はそのまま残置）

元データが18MB・8万行超あり、Excelパースに15〜17秒かかっていた。今後頻繁に修正・再起動を
繰り返す運用を見込み、ユーザーより「元データに変更がなければ次回起動を高速化したい」との
要望があり対応:

- パース結果を「元ファイルの隣（パス指定時）」または「OS一時フォルダ（アップロード時）」に
  `.kob1_cache/` としてpickle保存し、元ファイルのパス・更新日時・サイズが変わっていなければ
  次回起動時にそこから読み込む（実測: 初回15〜17秒 → ツール再起動後のキャッシュ利用時は約2.5秒）。
  キャッシュはリポジトリ外に置き、機密データが誤ってGit管理下に入らないようにしている
- 同一Streamlitプロセス内では`st.cache_data`により、フィルタ操作のたびの再読み込みも回避する
  （実測: 約1秒）
- 元データを更新すると更新日時・サイズの変化により自動的にキャッシュが無効化される。古い
  キャッシュファイルは新しいキャッシュの保存時に自動削除し、ディスク容量の圧迫を防止
- サイドバーに「キャッシュを無視して再読み込み」チェックボックスと、キャッシュ利用状況・
  保存先の表示を追加

## [20260722_03] - 2026-07-22

**追加ファイル:** `project_cost_analyzer_20260722_03.py`（`20260722_02`からのコピー＋機能追加。旧版はそのまま残置）

Step1「単一プロジェクトのコスト分析特化」として、PM・ファンクションマネージャー・製品部
ディレクターの3視点を踏まえた機能拡充を実施（4は予算対比で今回は保留）:

- 画面を3タブ構成に再編:
  - 🏛 事業部俯瞰: 全Profit Center（R03/R04/R07/R0N/R0S/R19）を横断俯瞰。事業部別サマリー表
    （総コスト・プロジェクト費/非プロジェクト費・プロジェクト数・平均/プロジェクト・内部労務比率）、
    プロジェクト費vs非プロジェクト費（`Project status = Other IO`で判定）の比較を追加
  - 🧭 プロジェクト深掘り: 単一プロジェクトのカルテ（累計コスト・内部労務比率・参加組織数・
    対象期間）、バーンチャート（月次コスト棒＋累計折れ線の二軸）、Function別/Organization別
    内訳、外部購買明細（PO単位）、工数投下(Man month, FY2026限定)を追加
  - 🔧 ファンクション横断: Function（職種）を選ぶと、どのプロジェクトに・誰が(Resource name)・
    いくら/何MMチャージしているかを横断表示
- 通貨表示を調査のうえUSDに統一。`Val/COArea Crcy`はSAPの統制領域通貨で元々単一通貨
  （ブック内「Cost by nature」シートが"Cost in $"と明示、Company Code別の金額規模からも
  現地通貨混在でないことを確認済み）のため換算は行わず、UIにUSDである旨を明示するのみ
- サマリーカードを「対象プロジェクト数 / 対象総コスト(USD) / 対象期間」の3枚に変更し、
  「明細行数」は削除
- 期間別コスト推移を折れ線から棒グラフに変更し、「積み上げ表示」トグル（Cost Category /
  Profit Center / PM cost categoryを軸に選択可）を追加
- Cost Category別・PM cost category別・Function別・Cost Element別（上位15）の内訳グラフに、
  個別に棒グラフ⇔円グラフを切り替えられるトグルを追加（円グラフはマイナス値＝会計調整分を除外）
- サイドバーのファイルパス・フィルタ選択（Profit Center/Fiscal Year/Project status）・
  積み上げ設定を、スクリプトと同じフォルダの`.pca_settings.json`に保存し、ツールを再起動
  しても前回の設定のまま復元されるようにした（`.pca_settings.json`はデータそのものは含まない
  設定値のみのため、`.kob1_cache/`と異なりGit管理下でも問題ないが、環境依存のローカルパスを
  含むためリポジトリには含めない運用とする）

## [20260722_04] - 2026-07-23

**追加ファイル:** `project_cost_analyzer_20260722_04.py`（`20260722_03`からのコピー＋機能追加。旧版はそのまま残置）

ユーザーが古いバージョン（`_02`相当の画面：折れ線グラフ・トグル無し）を見ていたが、
`_03`以降の機能拡充が反映されているように見えず混乱が生じた。原因はブラウザタブに古い
セッションが残っていたことだったが、「どのファイルが実際に動いているか」を`run_dashboard.bat`
のコンソール表示でしか確認できなかったため、アプリ画面自体からも一目で分かるようにした:

- サイドバー最上部に「🗂 実行中のファイル: `project_cost_analyzer_20260722_XX.py`」を常時表示。
  ブラウザだけを見て、意図したバージョンが起動しているか確認できるようにした

## [20260722_05] - 2026-07-23

**追加ファイル:** `project_cost_analyzer_20260722_05.py`（`20260722_04`からのコピー＋機能追加。旧版はそのまま残置）

`_03`で追加した棒グラフ⇔円グラフ切替トグルは「🏛 事業部俯瞰」タブの4グラフ（Cost Category別・
PM cost category別・Function別・Cost Element別）にのみ付けていたが、ユーザーが「🧭 プロジェクト
深掘り」タブでトグルが見当たらないと指摘。同タブのFunction別・組織(Organization)別・コスト種別
（内部労務 vs 外部購買）のグラフには付けていなかったための混乱だったので、一貫性を持たせるため
同タブの3グラフにも同じ`render_breakdown`（棒グラフ/円グラフ切替）を適用した。

## [20260722_06] - 2026-07-23

**追加ファイル:** `project_cost_analyzer_20260722_06.py`（`20260722_05`からのコピー＋バグ修正）

ユーザーより「ツールを立ち上げなおすと、プロジェクト深掘りタブ・ファンクション横断タブの
設定内容がリセットされる」と報告があり調査・修正:

- **選択中プロジェクト/Functionが保存対象に含まれていなかった**: `_03`で導入した設定保存
  （`.pca_settings.json`）は`s_path`/`s_profit_centers`/`s_fiscal_years`/`s_statuses`/
  `pf_stacked`のみが対象で、プロジェクト深掘りタブの選択プロジェクト(`s_project`)、
  ファンクション横断タブの選択Function(`s_function`)、および各グラフの棒/円グラフ・
  積み上げトグルの状態は保存対象外だった。永続化対象キーを`SETTINGS_DEFAULTS`という
  単一の定義にまとめ、これら全てを保存・復元するようにした
- **設定の保存タイミングが1手遅れていた**: `save_settings()`をタブ描画より前に呼んでいたため、
  プロジェクト/Functionを選び直した直後の1回は保存対象に反映されず、次に何か操作するまで
  保存されない状態だった。タブ描画後（その回の選択が確定した後）に保存するよう順序を修正
- **プルダウンの表示ラベルが復元されないStreamlit側の癖への対応**: 選択肢一覧が起動のたびに
  再生成されるため、`session_state`経由で値を復元しても、プロジェクト/Function選択の
  プルダウン表示ラベルだけが最初の選択肢のまま更新されない現象があった（実際の集計データは
  正しく復元されていたが、表示との食い違いで分かりにくかった）。ウィジェット自体のkeyを
  永続化対象のkeyと分離し、現在値から`index`を明示的に計算して渡す方式に変更し解消

## [20260722_07] - 2026-07-23

**追加ファイル:** `project_cost_analyzer_20260722_07.py`（`20260722_06`からのコピー＋機能追加）

Cost Categoryの深掘り機能を追加（当初「A: Function/Func.Category」「B: FSI Description」の
2軸で検討していたが、ユーザーより「C: B4P categoryも外せない」と追加指示があり4軸で実装）。
「🏛 事業部俯瞰」タブに新セクション「コスト種別 深掘り（内部労務 / 外部購買）」を追加:

- 分類軸セレクトボックスで4軸を切り替え: Function（内部労務）／Func.Category（内部労務・
  上位区分）／B4P category（外部購買・上位区分、9種）／FSI Description（外部購買、約28種）。
  Function/Func.CategoryはTime Writing(内部労務)行に、B4P category/FSI Descriptionは
  Material・Service(外部購買)行にそれぞれ絞り込んだ上で集計する（実データで確認済み。
  内部労務行にFSI Description等を使うとほぼ全件が同一カテゴリになり分解の意味が無いため）
  。既存の`render_breakdown`（棒/円グラフ切替）をそのまま流用
- 内訳グラフの項目を1つ選ぶと、その項目に該当する明細行が下部に表示される。プロジェクト名・
  PM・Cost Element名・Profit Center・Fiscal Yearでの絞り込み（複数選択）と、列を選んでの
  昇順/降順並び替えに対応。現在の「外部購買明細」（プロジェクト深掘りタブ、PO単位）は特定
  プロジェクトの深掘り向けであり全社俯瞰には粒度が細かすぎるとの指摘を受け、今回はCost
  Category切り口からの俯瞰的な明細参照として別に用意した
- 明細はExcelダウンロードにも対応
- 選択中の分類軸・項目も`.pca_settings.json`に保存し、次回起動時に復元する
- 実装中、`st.plotly_chart`に明示的な`key`を指定していないグラフ同士で
  `StreamlitDuplicateElementId`エラーが発生することが判明（同じ列構成の棒グラフが複数存在すると
  自動生成IDが衝突する）。既存分も含め全ての`st.plotly_chart`呼び出しに一意な`key`を付与して解消

## [20260722_08] - 2026-07-23

**追加ファイル:** `project_cost_analyzer_20260722_08.py`（`20260722_07`からのコピー＋機能追加）

`_07`のコスト種別深掘りは「🏛 事業部俯瞰」タブにしか付けていなかったが、ユーザーより
「🧭 プロジェクト深掘り」タブでも同じ機能を使いたいと要望があり対応:

- `render_axis_drilldown()`に`key_prefix`引数を追加し、同一画面内（Streamlitのタブは
  全タブ分が同一スクリプト実行内で描画されるため）で複数回呼び出してもウィジェットkey・
  永続化設定キーが衝突しないようにした（事業部俯瞰="dd"、プロジェクト深掘り="pdd"）
- プロジェクト深掘りタブに、選択中プロジェクトに絞り込んだ状態でのコスト種別深掘り
  （4軸切替＋フィルタ・並び替え付き明細テーブル）を追加。既存の「外部購買明細（PO単位）」
  セクションはPO単位という別の切り口のため、そのまま残置
- 選択中の分類軸・項目（`pdd_axis`/`pdd_value`）も設定保存の対象に追加

## [20260722_09] - 2026-07-23

**追加ファイル:** `project_cost_analyzer_20260722_09.py`（`20260722_08`からのコピー＋機能追加）

コスト種別深掘りの明細テーブルについて、ユーザーより2点要望があり対応:

- **フィルタ対象列をテーブルの列順（Index順）で全列に拡張**: これまでProject Name／PM／
  Cost element name／Profit Center／Fiscal Yearの5列のみで、Company Codeが対象外だった。
  金額列（`Val/COArea Crcy`、連続値のため複数選択フィルタに不向き）を除く全12列を、
  `DETAIL_TABLE_COLUMNS`の並び順のまま`DETAIL_FILTER_COLUMNS`とし、Company Code等も
  フィルタできるようにした（例: Company Code=CN69 かつ Fiscal Year=2025、のようなAND絞り込み）
- **「表示する列」を追加し、非表示にした列はフィルタからも連動して消えるようにした**:
  明細テーブルの直上に列選択の複数選択ウィジェットを新設。列を外すとテーブル表示だけでなく、
  フィルタ欄・並び替え列の選択肢からもその列が同時に消える（Streamlit標準の`st.dataframe`
  列非表示アイコンはPython側から状態を取得できないため、独自の列選択ウィジェットとして実装）

## [20260722_10] - 2026-07-23

**追加ファイル:** `project_cost_analyzer_20260722_10.py`（`20260722_09`からのコピー＋機能追加）

「今フィルタ・並び替えされている明細テーブルの状態を、時系列棒グラフや円グラフでも見たい」
との要望に対応。コスト種別深掘りの明細テーブル（フィルタ後の`work`）の直下に「この明細を
グラフ化」セクションを追加:

- 期間別コスト推移（既存の`render_period_bar`を再利用。積み上げ表示トグルも使える）
- 内訳の軸を選択できるセレクトボックス（フィルタ対象と同じ12列から選択）＋その軸での
  棒グラフ/円グラフ切替（既存の`render_breakdown`を再利用）。選択軸のユニーク値が20件を
  超える場合は上位20件に絞る（例: Project Nameのように件数が多い軸で円グラフの凡例が
  読めなくなるのを防止）
- いずれも既存コンポーネントの再利用のため新規ロジックは最小限。フィルタ・並び替えを変えると
  グラフも連動して更新される

## [20260722_11] - 2026-07-23

**追加ファイル:** `project_cost_analyzer_20260722_11.py`（`20260722_10`からのコピー＋修正・機能追加）

- **円グラフの並び順を修正**: Plotlyの円グラフは既定で反時計回りに配置されるため、
  「シェアの大きい順に12時から時計回りに並ぶ」という一般的な円グラフの見せ方になっていない
  との指摘を受けて修正。`render_breakdown()`（アプリ内の全ての円グラフが共通利用）で
  `sort=False`（データは`agg_by`で既にCost降順のため、Plotly側での再ソートを止める）と
  `direction="clockwise"`を明示指定し、大きい順に時計回りで並ぶようにした
- **「この明細をグラフ化」の内訳グラフを、上位20件の単純カットから「上位10＋その他」
  （標準）／「上位５＋その他」を選べる方式に変更**: `render_breakdown()`に`other_bucket`
  引数を追加し、`top_n`件目以降を「その他」として1件に集約できるようにした（`other_bucket`
  省略時は従来通りの単純カットで、他の呼び出し箇所には影響しない）。対象軸のユニーク値が
  5件を超える場合のみ「表示件数」ラジオボタン（上位10／上位5）を表示する
