# CHANGELOG

## v1.17.0 - daily_journal_20260806_06.py 「振り返りを書く」ボタンを独立したチップに変更
- 要望: v1.16.0でコントラストは直ったが、地味すぎて目立たない。ダサくない
  デザインで、もう少し目立つ配色にしてほしい
- 対応: ウィンドウと同色の「テキストリンク風」スタイルをやめ、独立した地色
  （`LKPT_TOGGLE_BG`、他のボタン群とは異なるラベンダー系）を持つチップ状の
  ボタンに変更した。太字化・内側の余白（padx=14, pady=5）も加えて、他の
  ボタンと並んでも埋もれないようにした
  - 地色をタグの選択状態（テーマ）から完全に切り離した固定色にしたため、
    v1.16.0で対応していた「タグ選択後の背景に対するコントラスト不足」の
    問題自体が起こり得なくなった（そもそも背景に依存しない設計にした）
- 動作確認: タグ未選択時・選択時・LKPT展開時のいずれでも、チップの配色が
  一定で視認性が保たれていることをXvfb上で確認した（高さも596→776pxと
  想定通りで、クリップは発生していない）

## v1.16.0 - daily_journal_20260806_05.py 「振り返りを書く」ボタンの文字色を修正
- 症状: タグ選択後、「✏️ 振り返りを書く（LKPT）」の文字が読みづらいという指摘が
  あった。実測するとコントラスト比1.56（WCAG基準4.5を大きく下回る）で、タグ未
  選択時の暗い背景ではACCENT_COLOR（ピンク）の文字が読めていたが、タグ選択後の
  淡い背景（pale_bg）に対しては別の色に切り替わっていなかったのが原因
- 対応: v1.14.0で選択中バッジ・時間帯プレビューに使ったのと同じ仕組みで、
  このボタンの文字色もタグ選択時のテーマ切替（`_apply_theme`）に追従させる
  ようにした（`_register_themed(..., fg=True)`を追加するだけの1行修正）。
  タグ未選択時はACCENT_COLOR、選択後はBUTTON_TEXT_COLOR（濃紺）に自動で
  切り替わる
- 動作確認: その他・NPIタグで選択後のボタン文字色を実測しコントラスト改善を
  確認した

## v1.15.0 - daily_journal_20260806_04.py ポップアップを2段化（中項目＋LKPT折り畳み）
- 要望: 「結局使わなくなってしまう」問題の相談ロードマップの②。毎時のポップアップ
  操作の負荷を下げるため、既定表示はタグ＋中項目の選択だけにし、LKPT（振り返り）
  は書きたい時だけ開く任意入力にしたい。中項目は以下の内容で運用する
  - R19 / JP Site: メール返信・データ整理・会議・Other（自由記述）
  - NPI: Caracal・Wheeling・GT・Other（自由記述）
  - その他: Other（自由記述）のみ（タグ自体が受け皿のため）
- 対応（storage.py）:
  - `SubItemMaster`シート（タグ名／中項目名／表示順）を新設。`TagMaster`と
    同じ考え方で、Excelを直接編集すれば中項目も自由に追加・変更できる
  - `TimeLog`シートに「中項目」列を追加（自己修復マイグレーション対応）
  - `record_check_in()`が`sub_item`引数を受け取るようになった（呼び出し側の
    破壊的変更）。`get_sub_item_master()`（読み取り専用、タグ別の中項目一覧を
    表示順で返す）を追加
- 対応（daily_journal_20260806_04.py）:
  - タグを選ぶと、その場で中項目ボタン（最大4個、2列グリッド）が現れる。
    「Other」を選ぶと自由記述欄が出る
  - LKPT（L/K/P/T）は既定で折り畳んでおき、「✏️ 振り返りを書く」を押した時
    だけ開く任意入力にした。開閉に合わせてウィンドウの高さも自動で変わる
  - 中項目が設定されているタグでは、中項目の選択を登録の必須条件にした
    （タグ選択のみ必須という既存のバリデーションと同じ扱い）
- 開発中に見つけて直した表示バグ:
  - 「Other（フリー記述）」という中項目ラベルが長すぎてボタン幅からはみ出し、
    ウィンドウ端で切れて見えていたため、ラベルを「Other」に短縮した
  - LKPTトグルボタンの背景をウィンドウと同色にしていたため、Tkinter既定の
    フォーカスリング（highlightthickness）が縁取りとして目立ってしまって
    いたのを`highlightthickness=0`で解消した
- 動作確認: 4タグすべてで中項目ボタンの表示、選択トグル、Otherの自由記述欄
  表示・値の解決、LKPT開閉に伴うウィンドウ高さの変化（596→776→596px、
  期待値と一致）、実際の登録（Excelへの書き込み）までXvfb上で確認した

## v1.14.0 - daily_journal_20260806_03.py 選択中バッジ・時間帯プレビューの文字色を修正
- 症状: 「選択中: R19」等のバッジと、その下の作業時間プレビューの文字が、
  タグの色によらず全体的に読みづらいという指摘があった
- 原因調査: WCAGのコントラスト比を実測したところ、選択中バッジは常に白文字
  固定だったため、4タグ中3タグ（R19: 2.09, JP Site: 3.07, その他: 2.72、
  いずれもWCAG AA基準の4.5を下回る）で不足していた。また時間帯プレビューは
  淡い水色固定だったため、タグ選択後の淡い背景（タグ色を白方向に明るくした
  色）に対して常にコントラスト不足だった
- 対応:
  - `_readable_text_color()`を追加し、背景色（タグの生の色）に対して白/濃紺
    のどちらがコントラスト比が高いかを実際に計算して選ぶようにした
    （`_relative_luminance()`/`_contrast_ratio()`もあわせて追加。WCAG式の
    実装のため、今後タグの色を追加・変更しても自動的に読みやすい方が選ばれる）
  - 時間帯プレビューの文字色を、淡い水色から濃紺（`BUTTON_TEXT_COLOR`）に
    変更した。この行はタグ選択後（＝淡い背景に切り替わった後）にしか
    表示されないため、濃紺固定で問題ない
- 動作確認: 4タグすべてで選択→バッジの文字色を実測し、白/濃紺の判定が
  期待通りになること、実際にスクリーンショットで視認性が改善したことを確認した

## v1.13.0 - daily_journal_20260806_02.py タグ選択時に記録される作業時間帯をプレビュー表示
- 要望: TIMEの分類（タグ）を選択する際に、それが何時から何時までの作業として
  これから記録されるのかを、登録前にHH:MM形式で確認したい
- 対応:
  - `storage.py`に`peek_next_time_range()`を追加した。record_check_in()内で
    使っていた「本日の直近チェックポイントから今回の作業時間範囲を計算する」
    ロジックを`_compute_time_range()`として切り出し、両方から共通で使うように
    リファクタリングした（書き込みは行わない読み取り専用関数のため、
    タグを何度選び直してもExcelへの影響はない）
  - タグを選択した瞬間（`_select_tag()`内）に`peek_next_time_range()`を呼び、
    「⏱ 08:00 〜 09:15 として記録されます」（本日最初のチェックインの場合は
    「⏱ 本日の基準点として記録されます」）と選択中バッジの下に表示するように
    した
  - プレビュー文言が2行に折り返した場合でも下のL/K/P/T欄やボタンが隠れない
    よう、`wraplength`とウィンドウ高さの見積もりを調整した
- ファイル名は`daily_journal_20260806_02.py`（同日2件目のバージョン）。
  `run_latest.py`は最新のdaily_journal_*.pyを自動選択するため、他ファイルの
  変更は不要

## v1.12.0 - daily_journal_20260806_01.py 入力ポップアップの摩擦を除去
- 症状・要望: 「結局使わなくなってしまう」問題の相談の中で、まず着手すべき
  「摩擦の除去」として次の4点が挙がった
  1. L/K/P/T入力欄が、表示側（ラベル・ボタン等）と異なる既定フォントで
     描画されており、サイズも書体も浮いて見える
  2. LKPTがそれぞれ何を意味していたか、毎回忘れてしまう
  3. 起動しているファイルが最新版かどうか、開いても一目で分からない
- 対応: `daily_journal_20260806_01.py`を新規作成し、以下を反映した
  - 入力欄(`tk.Entry`)に表示側と同じ`Yu Gothic UI`のフォントを明示指定した
    （フォント未指定によりTk既定フォントで描画されていたのが原因）
  - 各入力欄に薄字のプレースホルダ（L:「今日わかったこと」、K:「続けたい
    こと」、P:「気になっている課題」、T:「次に挑戦したいこと」）を常設し、
    フォーカスで消え、空のままフォーカスを外すと復帰するようにした
  - L/K/P/Tのラベル文字にホバーすると、正式な意味
    （Learned/Keep/Problem/Try）をツールチップで表示するようにした
    （`_Tooltip`クラスを追加）
  - ウィンドウタイトルおよび本文内に、起動中のバージョン
    （ファイル名から動的に取り出した`20260806_01`）を常に表示するように
    した。ハードコードした文字列ではなく`__file__`から動的に取り出すため、
    今後ファイルをリネームするだけで表示側も自動的に最新化される
- 動作確認: Xvfb上で実際にポップアップを描画し、初期状態のプレースホルダ
  表示、フォーカスでのプレースホルダ消去、入力後にフォーカスを外しても
  入力内容が保持されること、空のまま外すとプレースホルダに復帰すること、
  送信時の値取得（プレースホルダ表示中は空文字として扱われること）、
  ツールチップの表示、いずれも確認した

## v1.11.0 - dashboard.py COACHビューの表示をTextウィジェットで刷新
- 症状: COACHビューの本文が、①薄い色で読みづらい、②Geminiが返す
  `## 見出し`・`- 箇条書き`・`**太字**`の記号がそのまま生表示されて見づらい、
  ③ダッシュボード幅を変えても本文の折り返し幅（wraplength、固定px値）が
  追従しない、という3つの問題を抱えていた
- 対応: `tk.Label`（固定wraplength）から`tk.Text`（wrap="word"）に変更した
  - `tk.Text`は自身の実描画幅に合わせて自動的に折り返すため、ウィンドウを
    リサイズしても本文が追従するようになった（③の根本対応）
  - 高さは内容に応じて自動で持たないため、実際の折り返し後の表示行数
    （`count(..., "displaylines")`）を数えて`height`を設定する
    `_autofit_coach_text_height()`を追加。この関数はTextの`<Configure>`
    からも呼ばれるため、リサイズのたびに高さも再計算される
  - `## 見出し`→アイコン付きの見出しスタイル（`COACH_HEADING_ICONS`）、
    `- 箇条書き`→インデント付きの「•」箇条書き、`**太字**`→実際の太字、と
    Geminiの軽量マークダウンをパースして描画し、生の記号を画面に出さない
    ようにした（②の対応）
  - 本文の文字色を`BODY_TEXT`（淡いラベンダーグレー）から`TEXT_COLOR`
    （純白）に変更し、視認性を上げた（①の対応）

## v1.10.0 - dashboard.py COACHビューのプロンプトをブラッシュアップ
- 要望: v1.9.0で入れたCOACHビューのプロンプトについて、プロンプトエンジニアの
  視点でブラッシュアップの方向性を検討し、以下を反映してほしい
- 対応: `build_coach_prompt()`を全面的に書き直した
  - 出力を「## 要約 / ## パターンと停滞ポイント / ## コーチングアドバイス /
    ## 次の一歩」の4見出し固定にし、生成のたびに体裁がぶれないようにした
  - 「同じ課題（P）が形を変えて繰り返し記録されていないか」「Pに対応するT
    （次のアクション）が無いまま放置されていないか」など、記録間の
    パターン・PDCAの停滞箇所を明示的に探させる指示を追加した
  - 一般論・励ましの決まり文句を禁止し、記録内容を最低1つ具体的に引用する
    ことを必須にした
  - コーチングスタイルを「PDCA語彙で状況を診断しつつ、率直に踏み込む」
    （小さく早くPDCAを回して成長したいマネージャー向けに複数案から選定）
    に固定した
  - 末尾に「今週中に着手できる小さく具体的なアクション」を1〜2個、
    箇条書きで出す指示を追加した
  - 目安の文字数を400字→600字に緩和し、見出しごとの内容を詰め込みすぎない
    ようにした

## v1.9.0 - dashboard.py にCOACHビュー（Gemini APIによる期間要約）を追加
- 要望: CARD/FLOW/GROUPに続く4つ目のVIEWとして、その期間のL/K/P/T記録を
  要約し、シニアエグゼクティブコーチングアドバイザーとしての指南をもらえる
  ビューが欲しい
- 対応:
  - VIEWに「COACH」を追加（CARD/FLOW/GROUPと並べた時の文字数バランスも
    考慮した命名）
  - GEMINI_API_KEY環境変数 + gemini-2.5-flashモデルでGemini APIを呼び出す。
    新規ライブラリは追加せず、標準ライブラリのurllibでREST版を直接呼ぶ
    構成にした（`build_coach_prompt()` / `call_gemini_summary()`）
  - 生成タイミングは「その期間で初めてCOACHを選んだ時だけ自動生成し、
    以後はキャッシュを表示。明示的な「🔄 再生成」ボタンでのみ再実行する」
    というハイブリッド方式にした。同じ期間を何度見返してもAPIを叩き直さない
  - ネットワーク呼び出しはUIを固まらせないよう別スレッドで実行。Tkinterは
    メインスレッド以外からウィジェットを触れない制約があるため、結果は
    一度キュー（`queue.Queue`）に積み、メインスレッド側の`after()`ポーリング
    （`_poll_coach_queue`）で取り出す構成にした（開発中、ワーカースレッドから
    直接`root.after()`を呼んで"main thread is not in main loop"エラーになる
    不具合を検出し、この構成に修正した）
  - 期間・ビューの移動中に古いリクエストの結果が誤って反映されないよう、
    リクエストIDで新しい結果だけを採用するようにした
  - GEMINI_API_KEY未設定時・API呼び出し失敗時は、画面にエラー内容を表示する
    （アプリの起動やその他の機能には影響しない）

## v1.8.0 - dashboard.py のLKPTモードHISTORYをL/K/P/T色ベースに変更
- 症状: v1.7.0でLKPTモードの棒グラフはL/K/P/T分類にしたが、CARD/FLOW/GROUPの
  HISTORY一覧（色帯・ドット・タグチップ）は引き続きタグ色（TIMEモードの
  分類色）のままだったため、グラフと一覧の色の軸がずれて見づらかった
- 対応: LKPTモードのHISTORY（CARD/FLOW/GROUPすべて）を、タグ色ではなく
  L/K/P/T色を主役にする配色に変更した
  - L/K/P/Tバッジの地色を、無彩色（BADGE_BG）から棒グラフと同じ
    `LKPT_FIELD_COLORS`に変更した
  - CARDの左の色帯、FLOWのドットも、タグ色からL/K/P/T色に変更した
  - タグ名（R19等）はタグ色チップをやめ、プレーンテキストの補助情報として
    表示するようにした（`_tag_text()`を追加）。タグ色分類はTIMEモードの
    役割として残し、`_render_time_history()`（TIMEモードの一覧）は
    従来通りタグ色チップ（`_tag_chip()`）のままにした

## v1.7.0 - dashboard.py のLKPTモード棒グラフをL/K/P/T分類に変更
- 要望: LKPTモードとTIMEモードの棒グラフが両方ともタグ（R19/JP Site/NPI/
  その他）で分類されていたが、LKPTモードはL/K/P/Tで分類したい。TIMEモードは
  今まで通りタグ分類のままでよい
- 対応:
  - `aggregate_by_tag()`を`aggregate_lkpt_field_counts()`に置き換え、
    LKPTモードの棒グラフはL/K/P/Tそれぞれの件数を集計するようにした
    （タグ4色と混同しないよう、L/K/P/T用に別の固定配色`LKPT_FIELD_COLORS`
    を用意した）
  - TIMEモードの棒グラフ・円グラフは変更なし（タグ分類のまま）
  - v1.4.0で入れた棒グラフのクリック絞り込みも、LKPTモードはL/K/P/T単位、
    TIMEモードはタグ単位で効くように分岐した（`tag_filter`を汎用的な
    `chart_filter`に改名）。モードを切り替えると絞り込みは解除される
    （分類軸そのものが変わるため）
  - HISTORY側は元々v1.6.0でL/K/P/Tを1行ずつ展開する作りにしていたため、
    「Kだけ表示」のような絞り込みとも自然に噛み合う形になった

## v1.6.0 - dashboard.py のHISTORYをL/K/P/Tごとに1行表示へ変更
- 症状: CARD/FLOW表示で、1件のチェックインにL/K/P/Tが複数入っていると、
  1件が縦に何行にもなり、一覧の縦幅を無駄に消費していた
- 対応: L/K/P/Tを1件のカードにまとめず、値が入っている項目ごとに独立した
  1行として並べるようにした（`_flatten_fields()`を追加）
  - CARD: 色帯＋時刻＋タグ＋バッジ＋本文を1行に収めた
  - FLOW: 時刻＋ドット＋タグ＋バッジ＋本文を1行に収めた（縦の軸線は、行が
    短くなったことで見た目上の必要性が下がったため、このタイミングで
    ドット表示のみに簡略化した）
  - GROUP: 元々L/K/P/Tごとの区分けだったため、本文の折り返しを廃止して
    1行表示に揃えた
  - 本文が長い場合は折り返さず、表示幅に応じて省略記号（…）で切るように
    した（`_truncate_to_width()`を追加、ウィンドウ幅の変化にも追従する）

## v1.5.0 - dashboard.py に「今日に戻る」ボタンを追加
- 要望: ◀▶で前後の期間に移動できるようになったが、今どこまで戻れば今日に
  戻れるのか分かりにくい。スマートな今日復帰の手段が欲しい
- 対応: 今日を含まない期間を見ている時だけ、ウィンドウ右上に「● TODAY」の
  ピルボタンを浮かせて表示するようにした。クリックすると即座に今日（現在の
  期間種別のまま）に戻る。今日を含む期間を見ている間は表示自体が消えるため、
  普段の閲覧では画面が煩雑にならない
  - `period_frame`の中には置かず、独立した`place()`配置にすることで、
    ボタンの出現・消滅が◀ D W M Q Y ▶行の中心位置に影響しないようにした

## v1.4.0 - dashboard.py の棒グラフでタグ絞り込みができるように変更
- 要望: タグごとにバラバラに表示されるHISTORY一覧を、棒グラフをクリックして
  タグ単位で絞り込めるようにしたい。CARD/FLOW/GROUPすべてで使えること
- 対応: 棒グラフ（棒・数値・タグ名ラベルいずれもクリック対象）をクリックすると
  そのタグだけにHISTORYが絞り込まれるようにした。もう一度同じ棒をクリックすると
  解除される
  - 絞り込み中は、選択していないタグの棒を背景色寄りに沈めて選択タグを
    引き立てるようにした（`_blend_color()`を追加）
  - グラフ自体は期間内の全タグを表示したまま（絞り込みで棒が消えない）にし、
    タグの切替がグラフから直接できるようにした
  - 絞り込み中であることを示す常設のラベル（🔍 タグ名 のみ表示中）を
    サマリー行の下に追加し、クリックでも解除できるようにした。前後の期間へ
    移動して絞り込み中のタグの記録が0件になり棒自体が描かれなくなっても、
    このラベルから解除できることを確認済み
  - CARD/FLOW/GROUPの3つのHISTORY表示すべてで、絞り込み後の一覧・件数が
    正しく反映されることを確認した
- LKPTモードの記録件数の棒グラフだけでなく、TIMEモードの作業時間の棒グラフでも
  同じ仕組みで動作する（円グラフは対象外、要望どおり棒グラフのみ）

## v1.3.0 - dashboard.py にD/W/M/Q/Yの前後移動ボタンを追加
- 要望: 期間（D/W/M/Q/Y）を表示中、前後の期間（先週・翌週など）も見たい
- 対応: 期間ボタンの両端に◀（前へ）／▶（次へ）ボタンを配置した
  （並び: ◀ D W M Q Y ▶）
  - `_period_range()`の戻り値`end`は常に「次の期間の開始時刻」を表す性質を
    利用し、基準日（`reference_date`）を`end`に合わせれば次の期間へ、
    `start`の1日前に合わせれば前の期間へ移動できるようにした
    （D/W/M/Q/Yいずれも同じロジックで動く）
  - モード切替・期間切替時は`reference_date`を現在日時に戻さず維持する
    （前後移動した状態のままLKPT/TIME表示を切り替えられる）
- 併せて、VIEW（CARD/FLOW/GROUP）行のセンター位置がD/W/M/Q/Y行とズレて
  見えていた問題を修正: 従来「VIEW」ラベルをボタン行内に横並びで置いていたため
  ボタン列全体が右にずれて見えていた。ラベルを独立した行に分離し、
  ボタン行自体は左右対称にしたことで、上のD/W/M/Q/Y行と中心が揃うようにした

## v1.2.0 - run_latest.py 起動時に旧プロセスを自動終了するように変更
- 症状: コードを更新して起動し直しても、古いプロセスをKillしていないと、
  `RegisterHotKey`は同じキー組み合わせを1プロセスしか保持できないため、
  ホットキーは先に起動していた古いプロセスに奪われたままになり、
  「更新したのに動作が変わらない」という混乱の原因になっていた
- 対応: `_terminate_previous_instances()`を追加し、`main()`の先頭で必ず実行する
  ようにした。コマンドラインに"run_latest.py"を含む`python.exe`/`pythonw.exe`
  プロセスをPowerShell（`Get-CimInstance Win32_Process`）経由で列挙し、
  自分自身のPIDを除外して`taskkill /F`する
  - PowerShellが使えない環境でも起動処理自体は止めず、ログに記録するだけに
    留める（安全側に倒す設計）
  - これにより、`RunConsole.bat`で診断用に手動起動する際も、常駐中の
    スタートアップ側インスタンスを事前に手動で終了する必要がなくなった
    （v1.1.0のCHANGELOGに記載していた「先に常駐プロセスを終了してから使う」
    という注意書きは、この変更により不要になった）

## v1.1.2 - check_startup.py（自動起動の診断ツール）を追加
- 症状: v1.1.1で用意した`.bat`診断ツールが、日本語WindowsのコマンドプロンプトでUTF-8を
  CP932として解釈してしまい、コマンドとして認識できず全滅した
- 対応: 診断ロジックを`check_startup.py`（Python）に全面的に書き直し、`.bat`は
  それを1行呼ぶだけの最小限（ASCIIのみ）にした。診断ロジックはこちらの開発環境で
  実際にテストできるため、今後同種の文字化けは起きない
- 併せて、バッチファイル解析中に見つけたバグを修正: `start "" "python.exe" "script.py"`
  のように空のウィンドウタイトル引数`""`を挟む行を素朴な`"([^"]+)"`で読むと、
  引用符の対応がずれてPython実行ファイルのパスを正しく取り出せなかった。
  ドライブレター（`C:`等）で始まる区間だけを対象にする正規表現に変更した

## v1.1.1 - run_latest.py 起動失敗を検知できるようにした（自動起動の無反応対策）
- 症状: PC起動後、スタートアップからアプリが立ち上がっていなかった。
  しかも画面には何のエラーも出なかった
- 原因: スタートアップは`pythonw.exe`（コンソールを持たない）で起動するため、
  起動に失敗しても例外が表示されずに終わる。ファイル名変更に伴い
  スタートアップフォルダのバッチファイル（リポジトリ管理外のため手動更新が必要）が
  古いまま`popup_ui.py`を指していると、終了コード2で無言のまま終了する
- 対応:
  - `startup.log`に起動の成否を1行ずつ追記するようにした。
    「今朝ちゃんと起動したか」を後から確認できる
  - 起動に失敗した場合は、ログに加えてダイアログでも知らせるようにした
    （コンソールが無くても失敗に気づけるようにするため）
  - 起動対象の判定を`daily_journal_yyyymmdd_NN.py`に厳密一致する正規表現に変更。
    「daily_journal_20260805_01_backup.py」のような派生ファイルが
    最新版と誤判定される潜在バグを解消した（文字列の単純比較では
    `_`が`.`より後ろに並ぶため、バックアップの方が新しいと判定されていた）
- `.gitignore`に`startup.log`を追加

## v1.1.0 - dashboard.py 全面リデザイン（LKPT Dashboard化・一覧の作り直し）
- 名称変更
  - ウィンドウ名・見出し: 「学びダッシュボード」→「LKPT Dashboard」
  - モード切替: 「学び／時間」→「LKPT／TIME」
  - 期間切替: 「日次／週次／月次／四半期／年次」→「D／W／M／Q／Y」
  - 一覧見出し: 「📝 記録一覧」→「HISTORY」
  - 略号だけでは範囲が分からないため、`format_period_label()`を追加し、
    サマリー行に実際の範囲（例: `2026-08-05 – 08-11`, `2026 Q3`）を表示する
- フォントを全面的に拡大（`_init_fonts()`で一元管理）
  - タイトル14→16、ボタン既定→11、サマリー既定→12、
    グラフの数値8→11（等幅）、タグ名8→10、一覧本文→11、時刻→等幅10
- 円グラフに構成比（％）を表示
  - 扇が細いと内側に文字が収まらないため、`PIE_INSIDE_LABEL_MIN_PCT`（8%）
    未満の扇はラベルを円の外側へ逃がす
  - サマリー行と重複していた円グラフ下の「合計」表記は削除
- HISTORY（一覧）を`tk.Listbox`からCanvas＋スクロール可能なFrameに置き換え
  - Listboxは1行1色のプレーンテキストしか描けず、色・折り返し・複数行を
    扱えないため。入力ポップアップのタグ行と同じ作り方
  - VIEWトグルを追加し、LKPTモードの一覧の見せ方を切替可能にした。
    「何を軸に並べるか」を名前にしている
    - CARD（記録単位）: 1件を1枚のカードにまとめ、L/K/P/Tを1行ずつ表示
    - FLOW（時間軸）: 左に時刻軸を立て、タグ色のドットで打点。日付で区切る
    - GROUP（カテゴリ軸）: 時系列をやめ、LEARNED/KEEP/PROBLEM/TRYごとに束ねる
  - TIMEモードの一覧は形が固定のためVIEWトグルは隠し、所要時間そのものを
    横棒の長さで表す表示にした
  - ホイールスクロールに対応（領域に入った時だけ`bind_all`し、出たら解除）
- 一覧を「ログの羅列」から脱却させるため、中間色を追加
  （`CARD_BG` / `TRACK_BG` / `LINE_COLOR` / `DIM_TEXT` / `BODY_TEXT` / `BADGE_BG`）。
  タグ4色とACCENT_COLORは既存のまま変更なし
- LKPTモードはグラフ領域を250→180pxに縮小し、その分をHISTORYに回した

## v1.0.1 - ポップアップに「📊 DB」ボタンを追加し、3ボタンを均等幅に変更
- 症状: ポップアップからダッシュボードを開く手段が無く、別途手動で
  `dashboard.py`を起動する必要があった
- 対応: 「✅ 登録」「❌ キャンセル」の並びに「📊 DB」ボタンを追加し、
  クリックでダッシュボードを`Toplevel`として開くようにした
  - `_open_dashboard()`: 既にダッシュボードが開いていれば前面化のみ行い、
    多重起動しないようにした（`_show()`の既存パターンを踏襲）
  - `dashboard`モジュールは関数内で遅延importし、`BG_COLOR`等の同名定数が
    モジュールレベルで衝突しないようにした
- 3ボタンの幅を、`pack(expand=True, fill="x")`から`grid`の`uniform`カラムに
  変更し、テキスト長に関わらず完全に均等な幅になるようにした
  （`pack`の`expand`だけでは、ボタンごとの自然サイズの差が残ってしまうため）

## v1.0.0 - popup_ui.pyを日付連番ファイル名に変更し、固定名ランチャーを導入
- 今後、入力ポップアップ本体のファイル名を`daily_journal_yyyymmdd_NN.py`形式で
  バージョン管理する方針に変更。`popup_ui.py`を`daily_journal_20260805_01.py`に
  リネーム（git mvで履歴を保持）
  - ファイル内の自己参照（起動時ログの表示など）を`os.path.basename(__file__)`
    経由に変更し、以後ファイル名が変わってもコード修正不要にした
- `run_latest.py`を新規追加。`daily_journal_*.py`をファイル名の文字列比較で
  検索し、最も新しいものを動的にimportして`run()`を呼び出す固定名の
  ランチャー。バッチファイル側はこのファイルだけを指定しておけば、
  新しいバージョンのファイルを追加するだけで、バッチファイルの修正なしに
  常に最新版が起動する
- `RunConsole.bat`の起動対象を`popup_ui.py`から`run_latest.py`に変更
- `scheduler.py`の`write_startup_batch_file()`/`register_startup_task()`の
  既定の起動対象パスを`popup_ui.py`から`run_latest.py`に変更（本体のファイル名が
  今後変わっても、これらの既定値は変更不要になる）
- 注意: Windowsのスタートアップフォルダに既に配置済みの
  `LearningJournalAutoStart.bat`は、このリポジトリでは管理していない
  ローカルファイルのため、`run_latest.py`を指すよう手動で更新が必要

## v0.9.1 - 起動直後に一度ポップアップを自動表示するように変更
- 症状: Windowsスタートアップから自動起動しても、次の定時リマインド（毎時）まで
  何も画面に出てこないため、朝8時過ぎに出社してPCを立ち上げても9時まで
  基準点（本日最初のチェックイン）を促されない
- 対応: `popup_ui.run()`内で、`start_scheduler_loop()`呼び出し後に
  `queue_popup_trigger()`を1回呼び、起動直後にポップアップを自動表示するように
  変更した。既存の定時リマインドと同じトリガー機構（`_trigger_queue`）を
  再利用しているため、追加のロジックは不要だった

## v0.9.0 - 学びの1行メモをLKPT(Learned/Keep/Problem/Try)の4項目に拡張
- アジャイルのふりかえり手法を参考に、「今日の学び」の1行メモを
  L(Learned)/K(Keep)/P(Problem)/T(Try)の4つの独立した1行入力欄に拡張した
- `storage.py`:
  - `ENTRIES_HEADER`を`["日付","タグ","メモ"]`から
    `["日付","タグ","L","K","P","T"]`に変更
  - `_ensure_lkpt_columns(wb)`を新規追加。既存ブック（メモ1列の旧形式）を
    開いた際、メモ列をそのまま「L」列として読み替え、K/P/T列を自動追加する
    自己修復を行う。既存のメモの値は列の移動をせずそのまま残るため、
    データはそのままLとして統合される
  - `record_check_in()`の引数を`memo: str`から`l/k/p/t: str`の4つに変更
- `popup_ui.py`:
  - 見出しラベル・ウィンドウタイトルバーをともに「今日の学び」から
    「LKPT」に変更
  - 単一のメモ入力欄を、L/K/P/Tそれぞれのラベル＋1行入力欄（4行）に置き換え
  - ウィンドウ高さの見積もりを、入力欄が1→4行に増えた分だけ拡大
- `dashboard.py`:
  - `load_entries()`をL/K/P/Tの4列を読むよう変更（旧3列形式のファイルを
    移行前に開いても壊れないよう安全に読み取る）
  - 記録一覧の表示を、入力されているフィールドだけ「L:xxx K:xxx」のように
    連結して表示するよう変更

## v0.8.0 - タイムログ機能を追加（学び機能とは独立した作業時間トラッキング）
- WorkingHours的な「直前のチェックポイントから今まで、何をしていたか」を
  自己申告するタイムトラッキング機能を追加。設計の概要は下記の通り
  （詳細は `/root/.claude/plans/logical-giggling-quokka.md` の設計コンセプト参照）
  - タグ選択＝前回チェックポイントから今までの作業申告。メモは任意
  - 本日最初のチェックインは基準点マーカー（開始=終了=now、所要時間0分）を
    記録するのみで、作業記録は作らない
  - 前回チェックポイントからの経過が2時間(`MAX_TIMELOG_GAP_HOURS`)を超える
    場合は、記録される作業時間を2時間に打ち切る（長時間放置時の安全弁）
  - メモがあれば従来通りEntriesシートにも「学び」として記録する
- `storage.py`:
  - 新シート`TimeLog`（開始/終了/タグ）を追加。`ensure_workbook_exists()`が
    新規作成時にこのシートも作る。既存ブックには`_ensure_timelog_sheet()`で
    自己修復的に追加する
  - `record_check_in(tag, memo, path, now)`を新規追加。TimeLogとEntriesへの
    追記を1回のload/saveにまとめ、部分成功のリスクを避けた
- `popup_ui.py`:
  - 「✅ 登録」ボタンを追加（メモ空でも明示的に登録できるように）
  - `_submit()`のバリデーションをタグ選択のみに変更（メモは任意に）
  - 登録後、「✅ 基準点を記録しました」または「✅ ○○として記録しました(1h05m)」を
    一瞬表示してからポップアップを閉じる
  - Escapeキーが`_cancel()`を経由するよう統一
- `dashboard.py`:
  - 既存の期間切替ボタンと同じ見た目で「学び/時間」のモード切替ボタンを追加
  - `load_time_log_entries()` / `aggregate_time_by_tag()`を追加
  - 「時間」モードでは、同じCanvas領域を左右に分割し、棒グラフ（左）＋円グラフ
    （右、`create_arc`による手描き、追加ライブラリ導入なし）を表示する
  - 記録一覧は「[開始-終了] [タグ] (所要時間)」形式で表示（基準点マーカー行は
    所要時間0分のため一覧に表示しない）

## v0.7.4 - scheduler.py 定時リマインドを8:00〜18:00の毎時発火に変更
- 症状: これまでの定時リマインドは12:00と17:30の2回のみで、結局触らずに
  忘れてしまうとのフィードバックがあった
- 対応: `REMINDER_TIMES`を8:00〜18:00の正時11回（08:00, 09:00, ..., 18:00）に
  変更した。`check_reminders()`/`start_scheduler_loop()`のロジックは
  リストの要素数に依存しない実装のため変更なし

## v0.7.3 - 横幅の最小リサイズ幅を現在の2/3まで許容
- 症状: `resizable(True, True)`にしたが、`minsize(width, height)`の`width`が
  初期幅(300)そのものだったため、横方向には実質縮小できなかった
- 対応: `minsize`に渡す幅だけ`int(width * 2 / 3)`（=200）に変更し、横方向は
  現在の2/3まで縮小できるようにした。高さの最小値は変更なし

## v0.7.2 - ウィンドウ高さ見積もりの修正（キャンセルボタンの窮屈さ解消）
- 症状: v0.7.1で選択中バッジのフォントを大きく・太字にした結果、ウィンドウ高さの
  見積もり定数`chrome_height`が古いまま（170）で、実際に必要な高さより窓が
  小さくなり、下部のキャンセルボタンが窮屈に(潰れて)表示されていた
- 対応: `chrome_height`を170→210に、最小フォールバック高さを220→260に修正し、
  バッジの拡大分を反映した

## v0.7.1 - 選択中バッジの強調＋ウィンドウのリサイズ対応
- 症状: v0.7.0の背景色連動は、「選択中: ○○」ラベルまで薄色に染まってしまい、
  周囲に溶け込んで目立たなかった
- 対応: `selected_label`を一括テーマ切替(`_apply_theme`)の対象から外し、
  `_select_tag`内でタグの生の色（薄めていない元の色）を直接背景に設定するよう
  変更。文字も専用フォント(`Yu Gothic UI`, 13pt, 太字)＋パディングでバッジ状に
  強調した
- ウィンドウを`resizable(False, False)`から`resizable(True, True)`に変更し、
  ドラッグで縦横に自由にリサイズできるようにした
  - 内容が欠けないよう`minsize()`で最小サイズを保証
  - タグリスト部分(`tag_frame`)とメモ入力欄(`entry`)が拡大縮小に追従するよう
    `fill`/`expand`を追加

## v0.7.0 - popup_ui.py タグ選択を縦型リスト化＋選択色で背景が変わるように変更
- タグ選択UIを、横並びの塗りつぶしボタンから「色付きドット●＋タグ名」を
  縦に並べたリスト形式に変更
  - 行数（タグ件数）に応じてウィンドウの高さを自動計算するようにした
    （Excel側のタグ数が増減しても対応できる）
- タグを選択すると、ポップアップ全体の背景がダークネイビー(`#1a1a2e`)から、
  選択したタグ自身の色を白方向に薄めた色（同系色のパステルトーン）に
  切り替わるようにした。今どのタグを選んでいるかが背景色で一目で分かる
  - `_lighten_color(hex_color, factor)`: 色コードを実行時に薄める関数を追加。
    ハードコードした配色ではなく、Excel由来のタグ色から動的に計算するため、
    タグが何色・何件追加されても自動的に対応する
  - `_register_themed()` / `_apply_theme()`: 背景色・文字色をタグ選択に
    追従させたいウィジェットをまとめて管理する仕組みを追加
  - 色付きドット自体の色（タグ固有色）と、入力欄・キャンセルボタンの配色は
    テーマ切替の対象外とし、常に固定のまま
- 見出しラベルのテキストを「今日の気づき・学び」から「今日の学び」に統一
  （ウィンドウタイトルバーの表記と揃えた）
- `self.tag_buttons`（`tk.Button`の辞書）を`self.tag_rows`（行Frameの辞書）に変更

## v0.6.1 - RunConsole.bat 追加（コンソール表示付き起動用）
- `RunConsole.bat` を新規作成
  - `python.exe`（`pythonw.exe`ではなくコンソール表示あり）で`popup_ui.py`を
    起動し、終了時は`pause`でウィンドウを閉じずに残す
  - 診断用に毎回手動でコマンドプロンプトを開いてコマンドを打つ手間を省くための
    ダブルクリック起動用ファイル
  - 注意: `LearningJournalAutoStart.bat`経由の常駐プロセスと同時に実行すると、
    RegisterHotKeyは同じ組み合わせを2重登録できないため、後から起動した側は
    「❌ ホットキー登録に失敗しました」エラーコード1409になる。これは異常では
    なく、常駐側が先にホットキーを掴んでいる証拠。単体で切り分けたい場合は
    先に常駐プロセスを終了してから使う

## v0.6.0 - popup_ui.py ホットキー実装をWindows RegisterHotKey APIへ全面置き換え
- 症状: `Ctrl+Shift+J`によるポップアップ起動が、スタートアップ経由(pythonw.exe)の
  常駐プロセスでは機能しない。コンソールで手動実行した場合のみ動作していた
- 調査: 定時リマインド(scheduler.py、root.after()ベース)は問題なく動き続けており、
  keyboardライブラリの監視スレッドのみが機能していないと推測される状況だった。
  同一PCで動いていた別のAutoHotkeyスクリプト(`launch_launcher.ahk`)も確認したが、
  そちらは`Ctrl+Shift+L`で別スクリプトを起動するものであり、無関係と判明
- 対応: `keyboard`ライブラリへの依存を廃止し、Windows標準の`RegisterHotKey` API
  (`ctypes`経由)に置き換えた
  - `_hotkey_listener_loop()`: 専用スレッドで`RegisterHotKey`を登録し、
    `GetMessageW`でWM_HOTKEYメッセージを待ち受ける
  - `_parse_hotkey()`: `"ctrl+shift+j"`形式の文字列を修飾キーフラグと
    仮想キーコードに変換する簡易パーサーを追加
  - 登録に失敗した場合（他アプリが同じキーの組み合わせを登録済み等）は
    エラーコードを明示的にログ出力するようにした（従来は無言で失敗していた）
  - トリガーをキューに入れてTkinterメインスレッドでポーリングする既存の
    設計(`_trigger_queue` / `poll_trigger_queue`)はそのまま維持
- 依存削除: `keyboard`（`pip install keyboard`は不要になった）

## v0.5.4 - タグ配色を彩度の高いトーンに再調整
- `storage.py`: `DEFAULT_TAGS`をv0.5.3のパステルトーンから、指定の色見本
  （マゼンタ/ゴールド/グリーン/オレンジ）に近い、より発色の良い配色へ変更
  - R19 `#f6b8c8`→`#d9ae23`（ゴールド） / JP Site `#aed4ec`→`#2fa84f`（グリーン） /
    NPI `#b9e3c6`→`#c2399e`（マゼンタ） / その他 `#d7c6ea`→`#e08830`（オレンジ）
  - 注意: 既存のjournal_data.xlsxには自動反映されないため、
    TagMasterシートの「色コード」列を手動更新する必要あり（v0.1.1・v0.5.3と同様）
- `popup_ui.py`: タグボタンの文字色を`BUTTON_TEXT_COLOR`（濃紺）から`TEXT_COLOR`
  （白）に戻した。背景の彩度が上がったため白文字の方が視認性が高いため
- キャンセルボタン・期間切替ボタン・`ACCENT_COLOR`はv0.5.3のパステル配色のまま変更なし

## v0.5.3 - ボタン配色をパステルカラーに変更
- `storage.py`: `DEFAULT_TAGS`の色コードをパステルトーンに変更
  - R19 `#c96a7c`→`#f6b8c8` / JP Site `#3d6d99`→`#aed4ec` /
    NPI `#4d8b7c`→`#b9e3c6` / その他 `#7a5c99`→`#d7c6ea`
  - 注意: 既存のjournal_data.xlsxには自動反映されないため、
    TagMasterシートの「色コード」列を手動更新する必要あり（v0.1.1と同様）
- `popup_ui.py` / `dashboard.py`: `ACCENT_COLOR`をパステルローズ`#f4a6b8`に変更
  - パステル背景に白文字では視認性が落ちるため、ボタン文字色として
    濃紺の`BUTTON_TEXT_COLOR`(`#2b2b40`)を新設し、タグボタン・キャンセル
    ボタン・期間切替ボタンに適用
  - `popup_ui.py`のキャンセルボタン背景は`BG_COLOR`（背景に同化）から
    パステルグレーの`CANCEL_BTN_BG`(`#d8d8e6`)に変更し、ボタンとして
    視認できるようにした
  - `dashboard.py`の期間切替ボタン（未選択時）の背景は`PANEL_BG`（濃紺）から
    パステルブルーの`PERIOD_BTN_BG`(`#dfeaf5`)に変更
- ウィンドウ背景・入力欄・一覧表示（ダークテーマ）は変更なし

## v0.5.2 - scheduler.py 新機能（スタートアップフォルダ方式）
- 症状: `register_startup_task()`（schtasks方式）が「アクセスが拒否されました」で失敗
- 原因: 社内ポリシーによりコマンドラインからのタスクスケジューラ登録がブロックされていると推測
- 追加: `get_startup_folder()` / `write_startup_batch_file()` / `remove_startup_batch_file()`
  - 個人プロファイル内のスタートアップフォルダにバッチファイルを配置する方式に変更
  - 管理者権限・タスクスケジューラ権限が不要なため、より確実に動作する見込み

## v0.5.1 - scheduler.py 改善（ログオン時起動をコンソール非表示化）
- `register_startup_task()`が、同フォルダ内の`pythonw.exe`を自動検出して
  優先使用するよう変更
- 目的: ログオン時にコンソール画面が表示・点滅する違和感を解消するため

## v0.5.0 - dashboard.py 新規実装（全4モジュール完成）
- `dashboard.py` を新規作成
  - `load_entries()` / `load_tag_colors()`: Excelから記録・タグ色を読込
  - `filter_entries_by_period()` / `aggregate_by_tag()`: 日次/週次/月次/四半期/年次の絞込＋タグ別集計
  - `DashboardWindow`: 期間切替ボタン＋Canvasバーチャート＋記録一覧（ダークテーマ）
  - `run()`: 単体起動用エントリポイント
- 追加の外部ライブラリ導入なし（openpyxl・tkinter標準機能のみで実装）
- これにより storage.py / popup_ui.py / scheduler.py / dashboard.py の4モジュールが完成

## v0.4.0 - scheduler.py / popup_ui.py 重大バグ修正（二重import問題）
- 症状: 定時リマインド発火ログは出るが、ポップアップ表示処理(show())が
  一切呼ばれない（新規追加した検知ログ・例外ログも出ない）
- 原因: `python popup_ui.py`実行時、当該ファイルは`__main__`として動作するが、
  `scheduler.py`が`from popup_ui import queue_popup_trigger`を行うと
  同ファイルが別モジュール`popup_ui`として二重に読み込まれ、
  モジュール変数`_trigger_queue`が2系統に分裂していた
- 修正: `scheduler.py`から`popup_ui`のimportを完全に削除。
  `check_reminders()` / `start_scheduler_loop()`が`trigger_callback`を
  引数として受け取る方式に変更し、`popup_ui.py`の`run()`が
  自モジュールの`queue_popup_trigger`を直接渡すよう変更
- 影響: `scheduler.check_reminders()`および`scheduler.start_scheduler_loop()`の
  呼び出しシグネチャが変更（第一引数にtrigger_callbackが追加）

## v0.2.6 - popup_ui.py バージョン表示追加（ファイル更新有無の切り分け用）
- 起動時に「📦 popup_ui.py version: 0.2.5」を表示するよう変更
- 目的: 新しいログ（show()呼び出し検知、例外捕捉等）が全く出ない状況で、
  「コードが更新されていない」可能性と「更新されているが動作しない」可能性を切り分けるため

## v0.2.5 - popup_ui.py / scheduler.py 防御的ログ追加（原因切り分け用）
- 症状: 定時リマインド発火ログは出るが、以降何も起きず、エラーも表示されない
- 対応: 以下の箇所にtry/exceptと詳細ログを追加し、次回発生時に真因を特定できるようにした
  - `poll_trigger_queue()`: 例外発生時もログ出力しループを継続
  - `show()`: 呼び出し検知ログを追加、`get_tag_master()`失敗時のエラー捕捉を追加
  - `start_scheduler_loop()`: 例外発生時もログ出力しループを継続
- これによりエラーが起きた場合は必ずコンソールに表示されるようになった

## v0.2.4 - popup_ui.py バグ修正（定時リマインド時に前面表示されない問題）
- 症状: 定時リマインド発火のログは出るが、ポップアップが画面に表示されない
- 原因: Windowsのフォアグラウンド制限により、直前のユーザー入力が無い
  タイマー発火時は`focus_force()`が無効化されていた
- 修正: `show()`末尾を`_force_foreground()`に変更。topmost属性の解除→
  再設定を挟むことでWindowsの制限を回避

## v0.3.0 - scheduler.py 新規実装
- `scheduler.py` を新規作成
  - `check_reminders()`: REMINDER_TIMES（既定 12:00 / 17:30）と現在時刻が一致し、
    本日未発火なら強制ポップアップを発火
  - `start_scheduler_loop()`: root.after()による30秒間隔の監視ループ
  - `register_startup_task()` / `unregister_startup_task()`: schtasksコマンドによる
    ログオン時自動起動タスクの登録／削除
- `popup_ui.py` に公開関数 `queue_popup_trigger()` を追加（外部モジュールからの
  安全なポップアップトリガー用）
- `popup_ui.py` の `run()` 内で `scheduler.start_scheduler_loop()` を呼び出すよう変更
  （循環import回避のため関数内で遅延import）
- 未実装: `dashboard.py`（日次〜年次の集計表示）

## v0.2.3 - popup_ui.py UI調整（配色・操作性改善）
- 「記録する」ボタンを削除。Enterキー押下による記録（既存機能）のみに一本化
- 「↩️ 取消」ボタン→「❌ キャンセル」ボタンに変更
  - 変更前: 直前の保存済み記録をUndo
  - 変更後: 今回の入力を保存せず閉じる（保存済みデータには触れない）
- 未使用となった`undo_last_entry`のインポートを削除

## storage.py v0.1.1 - タグ配色の変更
- R19/JP Site/NPI/その他のデフォルト色をマイルドな配色に変更
  - NPIが背景色と同化していた問題を解消
- 注意: 既存のjournal_data.xlsxには自動反映されないため、
  TagMasterシートの「色コード」列を手動更新する必要あり

## v0.2.2 - popup_ui.py 切り分けテスト用変更
- 症状: Ctrl+Alt+L押下時、ホットキー検知ログ自体が出力されない
- 対応: HOTKEYを一時的に"ctrl+shift+j"に変更し、
  Ctrl+Alt+Lが本スクリプト外（OS/セキュリティソフト）で
  先取りされている可能性を切り分け

## v0.2.1 - popup_ui.py バグ修正（ポップアップ即消滅対策）
- 症状: `Ctrl+Alt+L`押下後、ポップアップが一瞬表示されてすぐ消える
- 原因: 修飾キー（Alt）が押されたままウィンドウがフォーカスを奪おうとし、
  Windowsがシステムメニュー呼び出しと誤認識してウィンドウを背面に送っていた
- 修正1: `poll_trigger_queue()` - ホットキー検知後、`root.after(150, popup.show)`で
  修飾キーの解放を待ってから表示するよう変更
- 修正2: `PopupWindow.show()` - `<Alt-KeyPress>`/`<Alt-KeyRelease>`をbreakで無効化し、
  表示直後に`lift()`＋`focus_force()`を明示実行

## v0.2.0 - popup_ui.py 新規実装
- `popup_ui.py` を新規作成
  - `register_global_hotkey()`: グローバルホットキー（既定 Ctrl+Alt+L）登録
  - `PopupWindow`: タグボタン（TagMasterから動的生成）＋1行入力欄のダークテーマポップアップ
  - `PopupWindow._submit()`: `storage.append_entry()`呼び出し→記録完了後に自動クローズ
  - `PopupWindow._undo()`: `storage.undo_last_entry()`呼び出しによる直近記録の取消
  - `poll_trigger_queue()` / `run()`: 別スレッド（keyboardフック）からのイベントを
    メインスレッドのTkinterに安全に伝搬するポーリング機構
- 依存追加: `keyboard`（グローバルホットキー検知用）
- 未実装: `scheduler.py`（定時リマインド）、`dashboard.py`（集計表示）

## v0.1.0 - storage.py 新規実装
- `storage.py` を新規作成
  - `ensure_workbook_exists()`: 初期ブック作成（Entries / TagMasterシート、初期タグ4種）
  - `append_entry()`: 1行追記（ロック競合時リトライ＋失敗時ローカル退避保存）
  - `undo_last_entry()`: 直近1件の取り消し
  - `get_tag_master()` / `add_tag()` / `remove_tag()`: タグの可変マスタ管理
- 未実装: `popup_ui.py`（入力ポップアップ）、`scheduler.py`（定時リマインド）、`dashboard.py`（集計表示）
