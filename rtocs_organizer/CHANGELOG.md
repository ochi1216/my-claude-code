# CHANGELOG — rtocs_organizer

このフォルダ内の変更履歴。バージョンアップ時は旧ファイルを残したまま新ファイルを追加し、ここに変更点を追記する。

## [20260719_01] - 2026-07-19

**変更ファイル:** `strategy_prompts.py`, `strategy_engine.py`, `strategy_report.py`, `README.md`
**追加ファイル:** `rtocs_dashboard_20260719_01.py`（`20260715_14`からのコピー＋バージョン更新。旧版はそのまま残置）

**緊急バグ修正**。ユーザーより「株式情報の参照先データ、特に数値データが間違っている」との報告があり調査した結果、2つの根本原因が判明:

1. **ティッカー誤認**: 会社分析ステージ(`STAGE1_COMPANY`)がGeminiの知識に基づいて推測する`ticker_candidates`は、実際に対象企業のものかを一切検証せず、株価データが取得できた（履歴が空でない）だけで無条件に採用していた。同名・類似名の別会社や親子会社違いのティッカーを誤って掴むと、PER/PBR/株価等の数値が丸ごと別会社のものになる
2. **配当利回りの単位変更未対応**: Yahoo Financeは2024年頃、`dividendYield`の単位を「小数(0.0234=2.34%)」から「%そのもの(2.34)」に変更したが、`strategy_report.py`側は旧仕様のまま無条件に100倍していたため、実際の利回りの100倍の値（例: 2.34%が234%）が表示されていた

**対応内容:**

- `STAGE1_COMPANY`の出力に`official_name_en`（英語表記の正式社名）を追加。ティッカー推測の確信が持てない場合は無理に候補を挙げず空配列にするよう指示を強化
- `strategy_engine.py`の`fetch_market_data()`が、取得した企業名(yfinanceの`longName`)と`official_name_en`を正規化して突合し、`name_match_confidence`(high/low/unknown)を付与。一致しない候補(low)は保留して次の候補を試し、他候補が無い場合のみ⚠️付きで返す（`_name_match_confidence()`, `_normalize_company_name()`新規追加）
- `_normalize_dividend_yield_pct()`を新規追加。値が1未満なら小数形式とみなして100倍、1以上ならすでに%形式とみなす自動判定に変更（`dividend_yield`→`dividend_yield_pct`にフィールド名変更。旧フィールドしか無い場合の後方互換表示も維持）
- 株式市場分析カードに、実際の取得元（Yahoo Financeの該当ティッカーページへの直接リンク）と取得企業名を常時表示。名称不一致時は⚠️警告ボックスで明示
- `GeminiClient.generate_grounded_json()`が、Google Search Groundingのレスポンスから実際に参照したURL（`grounding_metadata.grounding_chunks[].web`）を`grounding_sources`として抽出するようにした（`_extract_grounding_sources()`新規追加）。LLMにURLを生成させるとハルシネーションのリスクがあるため、API自体が返す検証可能なメタデータのみを使う
- レポートの`_guard()`共通処理に`grounding_sources`の描画を追加。ニュース・アナリスト洞察・マクロトレンドの各カードに「参照元（検索結果）」リンクが自動表示される（グラウンディングが機能しなかった場合は非表示のまま後方互換）

## [20260715_14] - 2026-07-18

**変更ファイル:** `strategy_prompts.py`, `strategy_engine.py`, `strategy_report.py`, `README.md`
**追加ファイル:** `rtocs_dashboard_20260715_14.py`（`_13`からのコピー＋バージョン更新。`_01`〜`_13`はそのまま残置）

MECE改善構想（5軸）の**軸5「TEMPORAL（時間軸・継続利用）」の項目①②**に対応。これでMECE5軸すべての優先実装項目（保留分を除く）に着手完了:

- `STAGE7_STRATEGY`の各戦略に`year_1_milestones`（1年後のマイルストーン）・`year_3_vision`（3年後のビジョン）を追加し、既存の`first_90_days`と合わせて3つの時間軸のロードマップを示すようにした（軸5-②）。ディープモードの批判・改訂パス(`STAGE7B_CRITIQUE`)は既存同様、同一スキーマを継承するため自動対応
- `strategy_report.py`の`generate_strategy_report()`が、HTMLレポートと同名のJSONサイドカーファイル（`Strategy_{企業名}_{タイムスタンプ}.json`、内部キー`_`始まりは除外）を`data/strategy_reports/`に自動保存するようにした。これが軸5-①の「過去レポート検出」の土台になる
- `strategy_engine.py`に`find_previous_report()`を新規追加。同一企業（ファイル名の safe_name 突合）の直近の過去レポート（JSONサイドカー）を検出する
- 新ステージ「⑪ 前回分析との比較（進捗トラッキング）」(`STAGE_PROGRESS_DIFF`、新規プロンプト)を追加。過去レポートが見つかった場合のみ実行し、前回の課題・戦略と今回を比較して「改善・解消した点」「新たな/悪化した課題」「戦略の継続性」「次のアクション」を生成する。過去レポートが無い場合（初回分析）は自動でスキップされ、追加コストは発生しない（軸5-①）
- レポートに「📈 前回分析との比較」カードを新設（戦略提言カードの直後。`progress`ステージが存在する場合のみ表示、旧バージョンの結果データでは非表示のまま後方互換）

## [20260715_13] - 2026-07-18

**変更ファイル:** `strategy_prompts.py`, `strategy_engine.py`, `strategy_report.py`, `README.md`
**追加ファイル:** `rtocs_dashboard_20260715_13.py`（`_12`からのコピー＋バージョン更新。`_01`〜`_12`はそのまま残置）

MECE改善構想（5軸）の**軸4「VALIDATION（検証・裏付けの厳密化）」の項目①②**に対応:

- `strategy_prompts.py`: 会社分析・ニュース・アナリスト洞察・マクロトレンド・株式市場・業界競合・他業種事例・課題分析・戦略策定の各ステージ出力スキーマに、新規フィールド`source_confidence`(high/medium/low)と`source_note`（確度の根拠）を追加。既存フィールドの構造は変更せず追加のみ（軸4-①）。類似RTOCS事例選定ステージ(`STAGE4_RETRIEVE`)は選定行為であり分析主張を含まないため対象外とした
- `strategy_report.py`: 共通の`_guard()`ヘルパーに出典確度バッジの描画を追加したため、上記全ステージのカードに自動で「出典確度」バッジ（✅高/🟡中/🔴低＋根拠）が表示される。フィールドが無い場合（旧バージョンの結果データ等）は何も表示しない後方互換設計
- `strategy_engine.py`: 戦略策定ステージ完了後に`_assess_financial_feasibility()`を実行し、各戦略のタイトル・根拠文にM&A・大規模投資等の規模語彙が含まれ、かつ既存の財務データ（軸1-⑤で取得済みの手元資金・FCF・負債資本比率）が乏しい場合、新規フィールド`feasibility_flag`に⚠️注記を追加する。追加のGemini呼び出しは発生しない（Pythonロジックのみ。軸4-②）
- レポートの戦略提言カードに`feasibility_flag`があれば警告ボックスとして表示

## [20260715_12] - 2026-07-18

**変更ファイル:** `strategy_engine.py`, `README.md`
**追加ファイル:** `rtocs_dashboard_20260715_12.py`（`_11`からのコピー＋バージョン更新。`_01`〜`_11`はそのまま残置）

MECE改善構想（5軸）の**軸3「OUTPUT（成果物・対話性）」の項目②**に対応（ユーザー指示: 標準はOFFの選択式にする）:

- `strategy_engine.py`の`StrategyEngine.run_pipeline()`に`stop_before_issues`引数を追加。Trueの場合、会社分析〜他業種事例分析までの前半ステージのみ実行して結果を返す（内部キー`_pending_continuation`/`_stopped_before_issues`で保留状態を保持。`strategy_report.py`は既知のキーのみ参照するため無害）
- `StrategyEngine.continue_pipeline(result, additional_note=None)`を新規追加。保留結果を受け取り、課題分析・戦略策定の後半ステージを実行して完成させる。`additional_note`（中間確認時の追加コメント・修正指示）を渡すと制約条件として必須順守で反映される
- 共通処理は`_run_late_stages()`として抽出（前半→後半どちらの経路でも同一ロジックを通る）
- 戦略分析タブに「分析前に中間結果を確認する（オプション、既定はOFF）」チェックボックスを追加。ONの場合、前半ステージ完了後に会社分析・ニュース・アナリスト洞察・マクロトレンド・株式市場・業界競合・他業種事例の中間結果を表示し、追加コメントを入力した上で「▶️ 後半を実行する」ボタンで課題分析・戦略策定を実行する（OFFの場合は従来通り一気通貫で自動実行）
- 軸3-③（複数シナリオ並列比較ビュー）は将来拡張として保留中（軸3は①②着手完了）

## [20260715_11] - 2026-07-18

**変更ファイル:** `strategy_prompts.py`, `strategy_engine.py`, `README.md`
**追加ファイル:** `rtocs_dashboard_20260715_11.py`（`_10`からのコピー＋バージョン更新。`_01`〜`_10`はそのまま残置）

MECE改善構想（5軸）の**軸3「OUTPUT（成果物・対話性）」の項目①**に対応:

- `strategy_prompts.py`に`STAGE_CHAT_FOLLOWUP`を新規追加（JSONモードは使わず自然文で回答させるプロンプト）
- `strategy_engine.py`の`GeminiClient`に`generate_text()`（プレーンテキスト応答、JSONモード不使用）を追加
- `strategy_engine.py`に`answer_followup_question(result, question, chat_history=None, ...)`を新規追加。生成済みの全分析結果を文脈として渡し、経営者からの追加質問に自然文で回答する（1問=1回のGemini呼び出し、stage="chat"で別集計）
- 戦略分析タブのレポート表示直後に「💬 レポートについて質問する」チャットUIを追加（`st.chat_message`/`st.chat_input`、会話履歴はセッション内で保持、新しい分析を実行すると履歴をリセット）
- 軸3-②（分析前の確認・修正チェックポイント）・軸3-③（複数シナリオ並列比較）は次の実装対象・将来拡張として保留中

## [20260715_10] - 2026-07-18

**変更ファイル:** `strategy_prompts.py`, `strategy_report.py`, `README.md`
**追加ファイル:** `rtocs_dashboard_20260715_10.py`（`_09`からのコピー＋バージョン更新。`_01`〜`_09`はそのまま残置）

MECE改善構想（5軸）の**軸2「PROCESS（思考法・創造性エンジン）」の項目②③④**に対応。追加のGemini呼び出しは発生しない（既存ステージのプロンプト強化のみ）:

- `STAGE7_STRATEGY`の3つの戦略それぞれに「大胆さのモード」(`mode`: conservative/ambitious/disruptive)を必須で1つずつ割り振らせるよう変更（軸2-②）。破壊的モードには「もし今日創業するなら」「もし最大の強みを捨てたら」「もしリソースが無限にあったら」という制約外し思考実験を明示的に組み込んだ
- `STAGE4_RETRIEVE`（類似RTOCS事例選定）の選定基準を「業種の一致よりビジネスモデル・課題構造の類似性を優先」に強化し、`is_cross_industry`フラグを新設。5〜8件のうち最低2件は異業種事例を選ぶよう努めさせた（軸2-③）
- `STAGE7_STRATEGY`の出力に`devils_advocate_note`（3戦略セット全体への最も辛辣な反論。最も失敗しそうな戦略とその理由）を追加。全モード共通・新規ステージ化なし（軸2-④）。ディープモードの批判・改訂パス(`STAGE7B_CRITIQUE`)は既存同様、同一スキーマを継承するため自動対応
- レポートの戦略提言カードに、色分けされたモードバッジ（🛡️保守的/🚀野心的/💥破壊的）と悪魔の代弁者の所見ボックスを追加。他業種RTOCS事例カードの選定テーブルに🌉異業種バッジを追加
- 軸2-⑤（複数ペルソナ討議）は将来拡張として保留中（軸2は①〜④着手完了）

## [20260715_09] - 2026-07-15

**変更ファイル:** `strategy_prompts.py`, `strategy_report.py`, `README.md`
**追加ファイル:** `rtocs_dashboard_20260715_09.py`（`_08`からのコピー＋バージョン更新。`_01`〜`_08`はそのまま残置）

MECE改善構想（5軸）の**軸2「PROCESS（思考法・創造性エンジン）」の項目①**に対応。追加のGemini呼び出しは発生しない（既存の戦略策定ステージのプロンプト強化のみ）:

- `STAGE7_STRATEGY`に4つの戦略フレームワーク（ブルーオーシャン戦略/バリューカーブ、ジョブ理論、アンゾフの成長マトリクス、ポートフォリオ経営/BCG的発想）を明示し、3つの戦略が単一の切り口に偏らないよう横断的に検討させる指示を追加
- 戦略の出力スキーマに`framework_lens`（どのフレームワークが発想の起点になったか）を追加。ディープモードの批判・改訂パス(`STAGE7B_CRITIQUE`)も同一スキーマを継承するため自動的に対応
- レポートの戦略提言カードに、`framework_lens`のバッジ（🧭）を表示

## [20260715_08] - 2026-07-15

**変更ファイル:** `strategy_engine.py`, `strategy_prompts.py`, `strategy_report.py`, `README.md`
**追加ファイル:** `rtocs_dashboard_20260715_08.py`（`_07`からのコピー＋バージョン更新。`_01`〜`_07`はそのまま残置）

MECE改善構想（5軸）の**軸1「INPUT（情報収集）」の項目⑤**に対応:

- `fetch_market_data()`が手元資金(`total_cash`)・総負債(`total_debt`)・負債資本比率(`debt_to_equity`)・フリーキャッシュフロー(`free_cash_flow`)をyfinanceから追加取得
- 株式市場分析ステージの出力に`financial_capacity_note`（M&Aや大規模投資に振り向けられる資金余力の所見）を新設。データが無い場合は「データ不足のため要確認」と明記させる
- レポートの株式市場分析カードに、財務実行可能性データの表と資金余力の所見を追加
- 軸4-②（財務整合性の機械的チェック：戦略提言のコスト規模と資金余力を比較して無理があれば⚠️注記）は、この財務データを土台にした後続タスクとして別途実施予定
- 軸1-③（VOC）・軸1-⑥（組織・人材シグナル）は将来拡張として保留中（軸1は全項目着手完了）

## [20260715_07] - 2026-07-15

**追加ファイル:** `rtocs_dashboard_20260715_07.py`（`_06`からのコピー＋バージョン更新。`_01`〜`_06`はそのまま残置）
**変更ファイル:** `strategy_prompts.py`, `strategy_engine.py`, `strategy_report.py`, `README.md`

MECE改善構想（5軸）の**軸1「INPUT（情報収集）」の項目④**に対応:

- 戦略分析タブに「経営の制約条件（任意）」入力欄を追加（負債covenant、取締会の性質等、AIには分からない経営者自身の制約をフリーテキストで入力）
- 入力された制約条件は課題分析(⑨)・戦略策定(⑩、ディープモードの批判・改訂パス含む)の全ステージのプロンプトに「必須順守」として明示的に注入される
- 業界・競合分析(⑥)・類似事例選定(⑦)にも参考情報として伝播（`target_summary`経由）
- レポートに「📌 経営者が明示した制約条件」カードを新設（先頭に表示）
- 軸1-③（顧客の声/VOC）は将来拡張として保留中

## [20260715_06] - 2026-07-15

**追加ファイル:** `rtocs_dashboard_20260715_06.py`（`_05`からのコピー＋バージョン更新。`_01`〜`_05`はそのまま残置）
**変更ファイル:** `strategy_prompts.py`, `strategy_engine.py`, `strategy_report.py`, `README.md`

MECE改善構想（5軸）の**軸1「INPUT（情報収集）」の項目②**に対応:

- 「マクロ・技術トレンド分析」ステージを新規追加（アナリスト洞察の直後、全体で9→10ステージに）
  - AI・生成AI等のテクノロジーが業界の競争構造をどう変えつつあるか
  - 地政学リスク（貿易摩擦、規制、サプライチェーン再編）
  - 規制・法制度の変化
  - 個社の競合状況ではなく「業界全体・時代全体に影響する構造変化」を扱う点が既存の業界分析ステージとの違い
- `STAGE3_INDUSTRY`（業界・競合分析）から`industry_trends`フィールドを削除し、対象企業固有の競合構造・ポジショニングに集中する役割に整理（マクロトレンドとの重複を排除）
- レポートに「🌊 マクロ・技術トレンド」カードを新設（アナリスト洞察カードの直後）

## [20260715_05] - 2026-07-15

**追加ファイル:** `rtocs_dashboard_20260715_05.py`（`_04`からのコピー＋バージョン更新。`_01`〜`_04`はそのまま残置）
**変更ファイル:** `strategy_prompts.py`, `strategy_engine.py`, `strategy_report.py`, `README.md`

ツールの位置づけ再確認（RTOCSデータを真似るのではなく、他社事例研究による自社改革の創造的戦略立案支援）を踏まえたMECE改善構想（5軸: INPUT/PROCESS/OUTPUT/VALIDATION/TEMPORAL）の第1弾。**軸1「INPUT（情報収集）」の項目①**に対応:

- 「アナリスト洞察・経営陣メッセージ収集」ステージを新規追加（会社分析→直近ニュースの直後、全体で8→9ステージに）
  - セルサイドアナリストのレーティング変更・目標株価修正とその理由
  - 決算説明会(Earnings Call)・投資家向け説明会(Investor Day)でのマネジメント自身の発言
  - 市場の評価と経営陣の自己認識の間のギャップに関する所見
  - 直近ニュース収集ステージと同じgoogle-genai検索グラウンディング方式を使用
- レポートに「📊 アナリスト洞察・経営陣メッセージ」カードを新設（直近ニュースカードの直後）
- 株価の生数値だけでなく市場参加者の解釈を戦略提言に反映できるようになった（従来はyfinanceの数値とAIの知識のみに依存していた）

## [20260715_04] - 2026-07-15

**追加ファイル:** `rtocs_dashboard_20260715_04.py`（`_03`からのコピー＋バージョン更新。`_01`〜`_03`はそのまま残置）
**変更ファイル:** `strategy_engine.py`, `requirements.txt`

実機検証で判明したバグ修正:

- 直近ニュース収集ステージが `400 google_search_retrieval is not supported. Please use google_search tool instead.` で必ず失敗する不具合を修正
  - 原因: `google-generativeai`（レガシー・Google公式に開発終了宣言済みのSDK）のグラウンディング機能は旧世代方式(`google_search_retrieval`)のみで、`gemini-2.5-flash`/`gemini-2.5-pro`が要求する新方式(`google_search`)を構成できなかった
  - 対応: ニュース収集ステージのみ、後継の統合SDK `google-genai` に切り替え、`google_search`ツールを使用するよう変更。他の7ステージ（JSONモード呼び出し）は`google-generativeai`のまま変更なし
- `requirements.txt`に`google-genai>=1.0`を追加（`google-generativeai`と共存）

## [20260715_03] - 2026-07-15

**追加ファイル:** `rtocs_dashboard_20260715_03.py`（`_02`からのコピー＋バージョン更新。`_01`/`_02`はそのまま残置）
**変更ファイル:** `strategy_prompts.py`, `strategy_engine.py`, `strategy_report.py`

実データでの試用フィードバックに基づく改善:

- 「課題分析」カードの根拠(evidence)を単一文字列から配列スキーマに変更し、1項目ずつ改行して表示するよう修正（読点区切りで1行に詰め込まれ読みにくかった問題を解消）
- 戦略パイプラインに「直近ニュース収集」ステージを新規追加（会社分析の直後、全体で7→8ステージに）。Geminiの検索グラウンディング機能を使い、対象企業の英語・日本語・中国語（簡体字）の名称表記を自動推定した上で、直近12ヶ月以内の重要ニュース（決算・M&A・経営陣交代・規制動向・地政学リスク等）を検索・要約する。結果は課題分析・戦略策定にも自動的に反映される
- レポートに「📰 直近ニュース」カードを新設（会社分析の直後）
- 検索グラウンディングは`google-generativeai`（Google公式に非推奨化済み）のベストエフォート実装のため、モデル世代によっては機能しない可能性がある旨をREADMEに明記

## [20260715_02] - 2026-07-15

**追加ファイル:** `rtocs_dashboard_20260715_02.py`（`_01`からのバグ修正版。`_01`はそのまま残置）

- 傾向分析タブの「キーワードの年次トレンド比較」で `NameError: name 'kw' is not defined` が発生する不具合を修正（キーワード頻度上位を取り出すリスト内包表記の誤り）
- Streamlitの `use_container_width` 非推奨引数（2025-12-31以降削除予定）を `width='stretch'` に置換

## [20260715_01] - 2026-07-15

**追加ファイル:** `rtocs_dashboard_20260715_01.py`, `rtocs_index.py`, `strategy_engine.py`, `strategy_prompts.py`, `strategy_report.py`

- 既存の検索用ダッシュボード（`rtocs_dashboard_20260502_04.py`）を土台に、3タブ構成の統合ダッシュボードを追加
  - 📋 一覧・検索（従来機能を踏襲）
  - 📈 傾向分析: 業界分布の年次推移、キーワード頻度・トレンド、地域ミックス、複数回登場企業の一覧、AI俯瞰総評（Geminiによる全ケース横断コメント）
  - 🎯 戦略分析: 企業名を1つ入力すると、会社分析→株式市場分析(yfinance)→業界・競合分析→類似RTOCS事例選定→他業種事例分析→課題分析→戦略策定、を自動実行し1枚のHTMLレポートを生成
- `rtocs_index.py`: `data/JSON_lake` から軽量なケース一覧（`data/rtocs_index.json`）を増分構築する共通部品。傾向分析と戦略分析の類似事例検索（LLM-as-retriever方式）の両方が参照する
- コスト集計・失敗時の部分レポート継続（既存organizerと同じ方針）を踏襲

## [20260711_01] - 2026-07-11

**変更ファイル:** `rtocs_organizer_202060517_01.py` → `rtocs_organizer_20260711_01.py`（リネーム）

- ファイル名を他ツールと同じ `yyyymmdd_連番` 命名規則に統一

## [202060517_01] - 2026-07-10

**追加ファイル:** `rtocs_organizer_202060517_01.py`, `requirements.txt`, `README.md`

- `selenium` 未インストール環境で `ModuleNotFoundError` の生Tracebackが表示される問題を修正。import部分を `try/except` で囲み、`pip install -r requirements.txt` を促す日本語メッセージを表示するよう変更
- `requirements.txt`（selenium, google-generativeai, PyMuPDF）を新規追加
- セットアップ手順（Chromeのリモートデバッグ起動、GEMINI_API_KEY設定、category_map.jsonの準備）を記載したREADME.mdを新規追加
