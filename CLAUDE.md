# CLAUDE.md

このファイルはリポジトリ直下のセッション管理ルールを定義する。

## リポジトリの性質

`my-claude-code` は越智さん個人の複数の独立したツール群を格納するモノレポである。
各ツールは自己完結したサブフォルダ（例: `rtocs_organizer/`, `po_database_organizer/`,
`shareflex_dashboard/`）を持ち、フォルダ内に `README.md` / `CHANGELOG.md` /
`requirements.txt` / バージョン付きスクリプト（`ツール名_yyyymmdd_連番.py`）を置く、
という既存の開発ルールがある（詳細は本ファイル末尾「開発ルール（バージョン管理）」を参照）。

このファイル、および `docs/PROJECT_STATUS.md` / `docs/SESSION_HISTORY.md` /
`docs/NEXT_TASK.md` は、リポジトリ直下に置きつつも、**現在アクティブな1プロジェクト**
（下記）のセッション引継ぎ管理に用いる。他のツール（rtocs_organizer等）の引継ぎは
従来通り `HANDOVER_*.md` 形式で管理されており、本ファイルの対象外。

### 現在アクティブなプロジェクト

- プロジェクト名: **PDF メール解析ツール開発**（開発対象フォルダ: `weekly_pdf_diff/`)
- 開発ブランチ: `claude/pdf-mail-analyzer-s01-0nx08o`
- 概要: Weekly Report メールをOneNoteに集約したPDF（89ページ、Weekly15件程度）を解析し、
  各Weeklyを直前のWeeklyと比較して追加・修正された文言を青太字化した別名PDFを生成するツール。
  詳細仕様は `weekly_pdf_diff/IMPLEMENTATION_PLAN.md` を参照。

---

## セッション管理ルール

### セッションの定義

- 1セッションとは、Claude Code Web上で新しく作成された1つの会話セッションを指す。
- 本プロジェクトは「1つの明確な目的＝1セッション」で進める。
- セッション番号は、新しいClaude Code Webセッションが作成された場合だけ増やす。
- 同じ会話セッション内で追加修正・調整・テスト・再修正を行っても、セッション番号は変更しない。
- 同じセッション内で S02、S03 などへ勝手に番号を進めない。

### 初回セッションの扱い

- S01は、管理ファイルの初期作成と、それに続く最初の実作業（同一セッション内）の両方を含む。
- 管理ファイル作成だけを独立したセッションとして記録しない。
- 初期設定完了時点ではセッション終了処理を行わない。
- 初期設定完了時点では、原則としてコミット・Pushを行わない。

### セッション中の追加依頼

- 同じセッション内の追加依頼は、現在のセッションの作業項目として扱う。
- 追加依頼ごとに `SESSION_HISTORY.md` / `NEXT_TASK.md` を更新しない。
- 追加依頼ごとにコミット・Pushを行わない。
- 軽微な修正・試行錯誤・テスト途中ではコミット・Pushを行わない。

### セッション終了処理

セッション終了処理は、ユーザーが以下を明示した場合だけ実施する。

- 「このセッションを終了する」
- 「引継ぎを作成する」
- 「次のセッションへ移る」
- 「セッション終了処理を行う」

個々の作業が完了しただけでは終了処理を開始しない。終了時には以下をまとめて1回だけ行う。

1. `PROJECT_STATUS.md` を必要に応じて更新する
2. `SESSION_HISTORY.md` に今回のセッションを1件だけ記録する
3. `NEXT_TASK.md` を次回セッション用に更新する
4. 差分とテスト結果を確認する
5. 必要な変更をまとめてコミットする
6. リモートブランチへPushする
7. 未コミット変更と未追跡ファイルを確認する
8. 次回のセッション番号・タイトル・開始プロンプトを提示する

### コミットとPush

コミット・Pushは以下の場合に限る。

- ユーザーが明示的に指示した場合
- チェックポイント保存をユーザーが指示した場合
- セッション終了処理を行う場合

初期設定完了直後や、実作業の途中経過ではコミット・Pushを行わない。

### 機密データの取り扱い（本プロジェクト固有）

対象PDFは社内の実メール（Weekly Report）であり、個人名・メールアドレス・電話番号・
社内プロジェクトコードネームなどを含む。以下を徹底する。

- 元PDFおよびその全文テキストをリポジトリへコミットしない。
- テスト・デバッグ用のPDFは実データではなく、合成（人工生成）した最小サンプルPDFを用いる。
- ドキュメント（IMPLEMENTATION_PLAN.md等）に実データの個人名・メールアドレス・電話番号を
  転記しない。構造・座標・フォント等の技術的知見のみを記録する。

---

## 開発ルール（バージョン管理）※リポジトリ全体の既存ルール

- **ファイル命名**: プログラムを更新する際は、ファイル名を `ツール名_yyyymmdd_連番.py`
  （例: `weekly_pdf_diff_20260729_01.py`）とする。同日に複数回更新する場合は連番を上げる。
- **旧バージョンの保持**: バージョンアップ時に旧ファイルは削除・上書きしない。新旧のファイルを
  フォルダ内に併存させる。
- **CHANGELOG**: 各ツールフォルダに `CHANGELOG.md` を置き、バージョンごとの変更点を記録する。
