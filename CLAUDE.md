# CLAUDE.md

このファイルは、Claude Code Web で本リポジトリを扱う際の運用ルールを定義する。
特に「Outlook オーガナイザー開発」プロジェクトを「1つの明確な目的＝1セッション」で進めるための引継ぎ運用を定める。

## セッション運用

* このプロジェクトは「1つの明確な目的＝1セッション」で進める。
* 1セッションとは、Claude Code Web上で新しく作成された1つの会話セッションを指す。
* セッション番号は、新しいClaude Code Webセッションを作成した場合だけ1つ増やす。
* 同じ会話セッション内で追加の修正、表示調整、エラー修正、テスト、再修正を依頼されても、セッション番号を変更しない（同じセッション内でS04、S05などへ番号を進めない）。
* 同じセッション内の追加依頼は、現在のセッションの作業項目として扱う。
* 各セッション開始時に、以下のファイルを必ず読む。
  * `CLAUDE.md`
  * `docs/PROJECT_STATUS.md`
  * `docs/SESSION_HISTORY.md`
  * `docs/NEXT_TASK.md`
* 原則として、`docs/NEXT_TASK.md` に書かれた目的、および同じセッション中にユーザーから追加された作業項目の範囲内で作業する。
* タスクの範囲を勝手に拡大しない。
* 関係のないファイルを変更しない。
* 不明点や確認できない事項を推測で補完しない。
* 確認できない事項は「未確認」と記載する。

## 管理ファイルの更新ルール

* 同じセッション内の追加依頼ごとに、`docs/SESSION_HISTORY.md` を更新しない。
* 同じセッション内の追加依頼ごとに、`docs/NEXT_TASK.md` を更新しない。
* `docs/PROJECT_STATUS.md` は、プロジェクト全体の状態に実質的な変化があった場合だけ更新する。
* 管理ファイルは、ユーザーが明示的にセッション終了を指示した際にまとめて更新する（詳細は「セッション終了処理」を参照）。
* `docs/SESSION_HISTORY.md` には、1つのClaude Code Webセッションにつき1件だけ記録する。

## コミットとPushのルール

* 細かな追加依頼ごとにコミットやPushを行わない。
* 軽微な修正、表示調整、試行錯誤、テスト途中では、原則としてコミットやPushを行わない。
* コミットまたはPushは、次の場合に限定する。

  1. ユーザーが明示的にコミットを指示した場合
  2. ユーザーが明示的にPushを指示した場合
  3. ユーザーがチェックポイント保存を指示した場合
  4. ユーザーがセッション終了処理を指示した場合

* コミットやPushが必要か不明な場合は、勝手に実行せずユーザーへ確認する。

## セッション終了処理

* 個々の修正作業が完了しただけでは、セッション終了処理を実施しない。
* セッション終了処理は、ユーザーが次のいずれかを明示した場合だけ実施する。

  * 「このセッションを終了する」
  * 「引継ぎを作成する」
  * 「次のセッションへ移る」
  * 「セッション終了処理を行う」

* セッション終了時に、以下をまとめて1回だけ行う。

  1. `docs/PROJECT_STATUS.md` を必要に応じて更新する。
  2. `docs/SESSION_HISTORY.md` に今回のセッションを1件だけ追記する。
  3. `docs/NEXT_TASK.md` を次回用に更新する。
  4. 差分とテスト結果を確認する。
  5. 必要な変更をまとめてコミットする。
  6. 現在のリモートブランチへPushする。
  7. 未コミット変更と未追跡ファイルを確認する。
  8. 次回のセッション番号、タイトル、開始プロンプトを提示する。

## セッションタイトル

* セッション開始時にだけ推奨セッションタイトルを提示する。
* タイトル形式は次のとおりとする。

  ```
  プロジェクト名 S連番 - セッション全体の目的
  ```

* セッション番号は `docs/SESSION_HISTORY.md` の最新番号に1を加える。
* 初回は `S01` とする。
* プロジェクト名の表記を途中で変更しない（本プロジェクトでは「Outlook オーガナイザー開発」で統一する）。
* 目的部分は簡潔に記載する。
* 同じセッション内の追加依頼では、セッション番号と基本タイトルを変更しない。
* セッションの目的が根本的に変わった場合は、勝手に次のS番号へ移行せず、新しいセッションへの切替えをユーザーに提案する。

## Git運用

* 現在のブランチを確認してから作業する。
* ユーザーから指示がない限り、既存の安定版機能を削除しない。
* コミット前に差分を確認する。
* テスト可能な変更はテストする。
* テストできなかった場合は理由を記録する。
* 秘密情報、APIキー、パスワード、認証情報をコミットしない。
