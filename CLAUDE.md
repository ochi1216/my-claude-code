# CLAUDE.md

このリポジトリは、越智さんが個人で運用する複数の業務効率化ツール（Python製）を格納するモノレポです。
ツールごとに独立したフォルダ（例: `rtocs_organizer/`, `po_database_organizer/`, `shareflex_dashboard/`）を持ち、
各フォルダ内に `README.md` と `CHANGELOG.md` を配置する運用がすでに存在します。

**Target Point Video Organizer** はこのリポジトリで新規に開始するプロジェクトです。
このプロジェクトについては、下記の「セッション管理ルール」に従って `docs/` 配下の管理ファイルで
進捗・引継ぎを管理します。

---

## セッション管理ルール（Target Point Video Organizer）

### セッションの定義

- 1セッションとは、Claude Code Web上で新しく作成された1つの会話セッションを指す。
- このプロジェクトは「1つの明確な目的＝1セッション」で進める。
- セッション番号は、新しいClaude Code Webセッションが作成された場合だけ増やす。
- 同じ会話セッション内で追加修正、調整、テスト、再修正を行っても、セッション番号を変更しない。
- 同じセッション内でS02、S03などへ勝手に番号を進めない。

### 初回セッションの扱い

- プロジェクト開始時のClaude Code Webセッション全体をS01として扱う。
- 管理ファイルの初期作成は、S01の開始準備に含める。
- 管理ファイル作成だけを独立したセッションとして記録しない。
- 初期設定完了後は、同じセッション内で実作業へ進む。
- 初期設定完了時点では、セッション終了処理を行わない。
- 初期設定完了時点では、原則としてコミットやPushを行わない。

### セッション中の追加依頼

- 同じセッション内の追加依頼は、現在のセッションの作業項目として扱う。
- 追加依頼ごとに `SESSION_HISTORY.md` を更新しない。
- 追加依頼ごとに `NEXT_TASK.md` を更新しない。
- 追加依頼ごとにコミットやPushを行わない。
- 軽微な修正、試行錯誤、テスト途中ではコミットやPushを行わない。

### セッション終了処理

セッション終了処理は、ユーザーが以下を明示した場合だけ実施する。

- このセッションを終了する
- 引継ぎを作成する
- 次のセッションへ移る
- セッション終了処理を行う

個々の作業が完了しただけでは、終了処理を開始しない。

終了時には以下をまとめて1回だけ行う。

1. `PROJECT_STATUS.md` を必要に応じて更新する
2. `SESSION_HISTORY.md` に今回のセッションを1件だけ記録する
3. `NEXT_TASK.md` を次のセッション用に更新する
4. 差分とテスト結果を確認する
5. 必要な変更をまとめてコミットする
6. リモートブランチへPushする
7. 未コミット変更と未追跡ファイルを確認する
8. 次回のセッション番号、タイトル、開始プロンプトを提示する

### コミットとPush

コミットまたはPushは、以下の場合に限る。

- ユーザーが明示的に指示した場合
- チェックポイント保存をユーザーが指示した場合
- セッション終了処理を行う場合

初期設定完了直後には、コミット・Pushを行わない。
実作業と管理ファイル更新は、セッション終了時にまとめて保存する。

---

## リポジトリ共通の開発ルール（既存プロジェクトから継承）

- **ファイル命名**: プログラムを更新する際は、ファイル名を `ツール名_yyyymmdd_連番.py` とする。同日に複数回更新する場合は連番（`_01`, `_02`...）を上げる。
- **旧バージョンの保持**: バージョンアップ時に旧ファイルは削除・上書きしない。新旧のファイルをフォルダ内に併存させる。
- **CHANGELOG**: 各ツールフォルダに `CHANGELOG.md`（Markdown形式）を置き、バージョンごとの変更点を記録する。
- APIキー・パスワード・認証情報はコミットしない（`.gitignore` で除外）。
- ユーザーの指示なしにマージ、リベース、リセットしない。
