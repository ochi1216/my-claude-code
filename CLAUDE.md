# CLAUDE.md

このリポジトリで Claude Code Web セッションを運用する際のルールを定義する。

## リポジトリ概要

複数の独立した業務効率化ツールを1つのリポジトリにまとめたモノレポ。各ツールは
ルート直下に専用フォルダを持ち、`ツール名_yyyymmdd_連番.py` 形式でバージョン管理する
（詳細は `README.md` の「開発ルール」節を参照）。

- `po_database_organizer/` — SharePoint上のPO関連書類カタログ化ツール
- `rtocs_organizer/` — 大前研一RTOCS講義スタイルの企業戦略分析ツール
- `shareflex_dashboard/` — Shareflex文書管理エクスポートの集計ダッシュボード
- ルート直下の `youtube_summary_list_*.py` — YouTube動画要約リストツール
- `HANDOVER_*.md` — 個別ツールの構想・引継ぎメモ（セッション管理ファイル導入以前の方式）

## プロジェクト: Project Cost developer開発

Project CostのKOB1シート（SAPのプロジェクトコスト実績明細）を分析するツールを、
上記と同じ規約（専用フォルダ・バージョン管理・README/CHANGELOG）で開発する。
詳細な現状は `docs/PROJECT_STATUS.md`、直近の作業内容は `docs/NEXT_TASK.md` を参照。

## セッション管理ルール

### セッションの定義

- 1セッションとは、Claude Code Web上で新しく作成された1つの会話セッションを指す。
- 本プロジェクトは「1つの明確な目的＝1セッション」で進める。
- セッション番号は、新しいClaude Code Webセッションが作成された場合だけ増やす。
- 同じ会話セッション内で追加修正・調整・テスト・再修正を行っても、セッション番号は変更しない。
- 同じセッション内でS02、S03などへ勝手に番号を進めない。

### 初回セッションの扱い

- 管理ファイル（`CLAUDE.md` / `docs/PROJECT_STATUS.md` / `docs/SESSION_HISTORY.md` /
  `docs/NEXT_TASK.md`）の初期作成は、最初のセッションの開始準備に含める。
- 管理ファイル作成だけを独立したセッションとして記録しない。
- 初期設定完了後は、同じセッション内で実作業へ進む。
- 初期設定完了時点では、セッション終了処理（下記）を行わない。
- 初期設定完了時点では、原則としてコミット・Pushを行わない。

### セッション中の追加依頼

- 同じセッション内の追加依頼は、現在のセッションの作業項目として扱う。
- 追加依頼のたびに `SESSION_HISTORY.md` / `NEXT_TASK.md` を更新しない。
- 追加依頼のたびにコミット・Pushを行わない。
- 軽微な修正・試行錯誤・テスト途中ではコミット・Pushを行わない。

### セッション終了処理

セッション終了処理は、ユーザーが以下のいずれかを明示した場合だけ実施する。

- このセッションを終了する
- 引継ぎを作成する
- 次のセッションへ移る
- セッション終了処理を行う

個々の作業が完了しただけでは終了処理を開始しない。終了時には以下をまとめて1回だけ行う。

1. `PROJECT_STATUS.md` を必要に応じて更新する
2. `SESSION_HISTORY.md` に今回のセッションを1件だけ記録する
3. `NEXT_TASK.md` を次のセッション用に更新する
4. 差分とテスト結果を確認する
5. 必要な変更をまとめてコミットする
6. リモートブランチへPushする
7. 未コミット変更・未追跡ファイルを確認する
8. 次回のセッション番号・タイトル・開始プロンプトを提示する

### コミットとPush

コミット・Pushは以下の場合に限る。

- ユーザーが明示的に指示した場合
- チェックポイント保存をユーザーが指示した場合
- セッション終了処理を行う場合

初期設定完了直後にはコミット・Pushを行わない。実作業と管理ファイル更新は、
セッション終了時にまとめて保存する。

## 変更してはいけないこと

- 初期化だけでセッションを終了しない
- 初期化だけを独立したセッションとして登録しない
- 同じセッション内でセッション番号を増やさない
- 細かな作業ごとにコミット・Pushしない
- 既存コードを無関係に変更しない
- APIキー・パスワード・認証情報をコミットしない
- ユーザーの指示なしにマージ・リベース・リセットしない
