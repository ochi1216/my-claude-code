# CLAUDE.md

このファイルは、Claude Code Web上でこのリポジトリを扱う際のセッション管理ルールを定めるものです。
コード本体の設計方針や開発ルールは [`README.md`](README.md) を参照してください。

# セッション管理の基本原則

- このプロジェクトは「1つの明確な目的＝1セッション」で進める。
- 1セッションとは、Claude Code Web上で新しく作成された1つの会話セッションを指す。
- セッション番号は、新しいClaude Code Webセッションが作成されたときだけ1つ増やす。
- 同じ会話セッション内では、追加の修正依頼、表示調整、エラー修正、テスト、再修正を受けても、セッション番号を変更しない。
- 同じセッション内の細かな作業を、S04、S05などの別セッションとして記録しない。
- 同じ目的に属する実装、調整、テスト、再修正は、同一セッション内で継続してよい。
- セッションの目的が根本的に変わる場合は、勝手に次のセッション番号へ移行せず、ユーザーに新しいセッションへの切替えを提案する。

# セッション開始時

- 各セッション開始時に、以下のファイルを必ず読む。

  - `CLAUDE.md`
  - `docs/PROJECT_STATUS.md`
  - `docs/SESSION_HISTORY.md`
  - `docs/NEXT_TASK.md`

- 作業開始前に、現在のブランチ名とGitの状態を確認する。
- `docs/NEXT_TASK.md`およびユーザーの今回の指示を確認する。
- ユーザーの最新の明示的な指示を、`NEXT_TASK.md`より優先する。
- 今回のセッション全体の目的、対象範囲、完了条件を整理する。
- タスクの範囲を勝手に拡大しない。
- 関係のないファイルを変更しない。
- 不明点や確認できない事項を推測で補完しない。
- 確認できない事項は「未確認」と記載する。

# セッションタイトル

- セッション開始時にだけ、推奨セッションタイトルを提示する。
- タイトル形式は以下とする。

`プロジェクト名 S連番 - セッション全体の目的`

- セッション番号は、`SESSION_HISTORY.md`に記録された最新番号に1を加える。
- 初回は`S01`とする。
- プロジェクト名の表記を途中で変更しない。
- タイトルの作業内容は、セッション全体の目的を簡潔に表す。
- 同じセッション内で追加依頼を受けても、セッション番号と基本タイトルを変更しない。
- 同じセッション内でS04、S05などへ番号を進めない。

# セッション中の追加依頼

- 同じClaude Code Webセッション内で受けた追加依頼は、現在のセッション内の作業項目として扱う。
- 追加依頼ごとに新しいセッション番号を付けない。
- 追加依頼ごとに`SESSION_HISTORY.md`を更新しない。
- 追加依頼ごとに`NEXT_TASK.md`を更新しない。
- 追加依頼ごとにコミットやPushを行わない。
- 軽微な修正、表示調整、試行錯誤、テスト途中では、原則としてコミットやPushを行わない。
- 追加作業が完了した場合は、変更内容とテスト結果だけを簡潔に報告する。
- セッション終了処理は、ユーザーが明示的に指示するまで実施しない。

# コミットとPush

コミットまたはPushは、以下の場合に限り実施する。

1. ユーザーが明示的に「コミットしてください」と指示した場合
2. ユーザーが明示的に「Pushしてください」と指示した場合
3. ユーザーが「チェックポイントを保存してください」と指示した場合
4. セッション終了処理を行う場合
5. 一つの独立した大きな機能が完成し、安全な保存地点が必要であり、ユーザーの同意を得た場合

- 細かな作業ごとにコミットやPushを繰り返さない。
- コミットが必要か不明な場合は、勝手にコミットせずユーザーへ確認する。
- 1セッション内で複数回コミットする場合は、意味のあるチェックポイントに限定する。
- コミット前に差分を確認する。
- Push前に対象ブランチを確認する。
- 秘密情報、APIキー、パスワード、認証情報をコミットしない。

# セッション終了処理

セッション終了処理は、以下の場合にのみ実施する。

- ユーザーが「このセッションを終了する」と指示した場合
- ユーザーが「引継ぎを作成する」と指示した場合
- ユーザーが「次のセッションへ移る」と指示した場合
- ユーザーが明示的に終了処理を依頼した場合

個々の修正作業が完了しただけでは、セッション終了処理を実施しない。

セッション終了時に、以下をまとめて1回だけ実施する。

1. 今回のセッション全体の変更内容を確認する。
2. 必要に応じて`docs/PROJECT_STATUS.md`を更新する。
3. `docs/SESSION_HISTORY.md`に今回のセッションを1件だけ追記する。
4. `docs/NEXT_TASK.md`を次のセッション用に更新する。
5. 変更したファイルを一覧表示する。
6. 実施したテストと結果を報告する。
7. 未実施または未確認の項目を明記する。
8. Git差分を確認する。
9. 必要な変更をまとめてコミットする。
10. 現在のリモートブランチへPushする。
11. 未コミット変更と未追跡ファイルが残っていないか確認する。
12. 次回のセッション番号と推奨タイトルを提示する。
13. 次のセッションに貼り付ける短い開始プロンプトを作成する。

# Git運用

- 現在のブランチを確認してから作業する。
- 作業開始時に、リモートブランチとの差分を確認する。
- リモートと差分がある場合は、勝手にマージ、リベース、リセットを行わず、内容を報告してユーザーの指示を仰ぐ。
- ユーザーから指示がない限り、既存の安定版機能を削除しない。
- ユーザーから指示がない限り、既存の成果物ファイルを上書きしない。
- バージョン別ファイル運用が指定されている場合は、新しいバージョンファイルとして保存する。
- テスト可能な変更はテストする。
- テストできなかった場合は、その理由と未確認範囲を記録する。
- 秘密情報、APIキー、パスワード、認証情報をコミットしない。
