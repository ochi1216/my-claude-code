# CLAUDE.md

このリポジトリで Claude Code Web セッションを使って開発する際の共通ルール。

## リポジトリ概要

複数の社内ツール（Python/Flask 中心）を1リポジトリで管理するモノレポ。
各ツールはフォルダ単位で分離され、フォルダごとに `README.md` / `CHANGELOG.md` /
`requirements.txt` を持つ。バージョン管理・ファイル命名規則はルート
`README.md` の「開発ルール」節を参照。

現在管理しているツール（未確認＝本セッションで内容を精査していないもの）：

- `onenote_report_generator/` — OneNote議事録の自動要約レポート生成（本セッションで新規導入）
- `r19_site_organizer/` — SharePointサイトのフォルダツリー閲覧・ダウンロード（社内Windows PC運用ツールから本セッションで移管）
- `po_database_organizer/` — SharePoint PO書類のカタログ化（未確認）
- `rtocs_organizer/` — BBT RTOCS Organizer（未確認）
- `shareflex_dashboard/` — Shareflex Document Dashboard（未確認）
- `youtube_summary_list_*.py` — YouTube要約リスト（未確認）
- `HANDOVER_analog_ic_scout.md` / `HANDOVER_youtube_summary_list.md` — 他プロジェクトの引継ぎ資料（未確認）
このファイルはリポジトリ直下のセッション管理ルールを定義する。

## リポジトリの性質

`my-claude-code` は越智さん個人の複数の独立したツール群を格納するモノレポである。
各ツールは自己完結したサブフォルダ（例: `rtocs_organizer/`, `po_database_organizer/`,
`shareflex_dashboard/`）を持ち、フォルダ内に `README.md` / `CHANGELOG.md` /
`requirements.txt` / バージョン付きスクリプト（`ツール名_yyyymmdd_連番.py`）を置く、
という既存の開発ルールがある（詳細は本ファイル末尾「開発ルール（バージョン管理）」を参照）。

このファイル、および `docs/PROJECT_STATUS.md` / `docs/SESSION_HISTORY.md` /
`docs/NEXT_TASK.md` は、リポジトリ直下に置きつつも、**現在アクティブな1プロジェクト**
（下記）のセッション引継ぎ管理に用いる。他のツール（rtocs_organizer等）の引継ぎは
従来通り `HANDOVER_*.md` 形式で管理されており、本ファイルの対象外。

### 現在アクティブなプロジェクト

- プロジェクト名: **PDF メール解析ツール開発**（開発対象フォルダ: `weekly_pdf_diff/`)
- 開発ブランチ: `claude/pdf-mail-analyzer-s01-0nx08o`
- 概要: Weekly Report メールをOneNoteに集約したPDF（89ページ、Weekly15件程度）を解析し、
  各Weeklyを直前のWeeklyと比較して追加・修正された文言を青太字化した別名PDFを生成するツール。
  詳細仕様は `weekly_pdf_diff/IMPLEMENTATION_PLAN.md` を参照。

---
このリポジトリで Claude Code Web セッションを運用する際のルールを定義する。


複数の独立した業務効率化ツールを1つのリポジトリにまとめたモノレポ。各ツールは
ルート直下に専用フォルダを持ち、`ツール名_yyyymmdd_連番.py` 形式でバージョン管理する
（詳細は `README.md` の「開発ルール」節を参照）。

- `po_database_organizer/` — SharePoint上のPO関連書類カタログ化ツール
- `rtocs_organizer/` — 大前研一RTOCS講義スタイルの企業戦略分析ツール
- `shareflex_dashboard/` — Shareflex文書管理エクスポートの集計ダッシュボード
- ルート直下の `youtube_summary_list_*.py` — YouTube動画要約リストツール
- `HANDOVER_*.md` — 個別ツールの構想・引継ぎメモ（セッション管理ファイル導入以前の方式）

## プロジェクト: Project Cost developer開発

Project CostのKOB1シート（SAPのプロジェクトコスト実績明細）を分析するツールを、
上記と同じ規約（専用フォルダ・バージョン管理・README/CHANGELOG）で開発する。
詳細な現状は `docs/PROJECT_STATUS.md`、直近の作業内容は `docs/NEXT_TASK.md` を参照。

## セッション管理ルール

### セッションの定義

- 1セッション＝ Claude Code Web 上で新しく作成された1つの会話セッション。
- 「1つの明確な目的＝1セッション」で進める。
- セッション番号は、新しい Claude Code Web セッションが作成された場合だけ増やす。
- 同じ会話セッション内で追加修正・調整・テスト・再修正を行っても、セッション
  番号は変更しない（S02, S03 へ勝手に進めない）。
このファイルは、このリポジトリでClaude Code (CLI / Web) が作業する際の
プロジェクト運用ルールを定義する。

## リポジトリの性質

- このリポジトリ (`my-claude-code`) は、複数の独立した社内向けPythonツールを
  `ツール名/` フォルダ単位で管理するモノレポである。
- 各ツールフォルダは基本的に独立しており、共通のライブラリ/フレームワークには依存しない。
- 既存ツール例: `po_database_organizer/`, `rtocs_organizer/`, `shareflex_dashboard/`

## 既存の開発ルール（バージョン管理・ファイル命名）

`README.md` に記載の既存ルールを引き続き適用する。

- **ファイル命名**: プログラムを更新する際は、ファイル名を
  `ツール名_yyyymmdd_連番.拡張子`（例: `emergency_alert_tool_20260729_01.py`、
  `provision_sharepoint_20260730_01.ps1`）とする。`.py`に限らず、`.ps1`等の
  実行コードファイル全般に適用する。同日に複数回更新する場合は連番
  （`_01`, `_02`...）を上げる。
  - 例外: ダブルクリックで起動する「ランチャー」用の`.bat`ファイルは、
    ファイル名からその時点の最新リビジョンを自動検出して起動する設計とし、
    `.bat`自体はバージョン番号を付けない安定したファイル名のまま置き換える
    （例: `run_emergency_alert_tool.bat`, `run_power_automate_tools.bat`）。
- **旧バージョンの保持**: バージョンアップ時に旧ファイルは削除・上書きしない。
  新旧のファイルをフォルダ内に併存させる。
- **CHANGELOG**: 各ツールフォルダに `CHANGELOG.md` を置き、バージョンごとの
  変更点を記録する。
- **秘密情報**: `config.example.json` にはプレースホルダのみを記載し、
  実際のテナントID・クライアントシークレット等の秘密情報はコミットしない
  （`.gitignore` で実ファイル名の `config.json` 等を除外する）。
- **バッチファイル(.bat)の文字コード**: Windows起動用の`.bat`ファイルには、
  日本語などの非ASCII文字を含めない。過去に、UTF-8で保存した`.bat`内の日本語が、
  ユーザー環境のコンソールコードページ(Shift-JIS/CP932)で誤って解釈され、
  「'...' は、内部コマンドまたは外部コマンド...として認識されていません」という
  エラーでバッチファイルが起動しない事例が発生した
  （`emergency_alert_tool/run_emergency_alert_tool.bat`、2026-07-29）。
  再発防止のため、`.bat`ファイルの表示メッセージは英数字(ASCII)のみとし、
  改行コードはCRLFで保存する。日本語の説明が必要な場合は、`.bat`から呼び出す
  スクリプト側(`.ps1`/`.py`等、UTF-8を正しく扱える)か、別途Markdown文書に書く。


このプロジェクト（緊急連絡ツールの開発）は、下記のセッション管理ルールに従って進める。


- 1セッションとは、Claude Code Web上で新しく作成された1つの会話セッションを指す。
- このプロジェクトは「1つの明確な目的＝1セッション」で進める。
- セッション番号は、新しいClaude Code Webセッションが作成された場合だけ増やす。
- 同じ会話セッション内で追加修正、調整、テスト、再修正を行っても、セッション番号を変更しない。
- 本プロジェクトは「1つの明確な目的＝1セッション」で進める。
- 同じ会話セッション内で追加修正・調整・テスト・再修正を行っても、セッション番号は変更しない。
- 同じセッション内でS02、S03などへ勝手に番号を進めない。

### 初回セッションの扱い

- S01は、管理ファイルの初期導入と最初の実作業（自動送信機能の実装）の両方を含む、
  今回のClaude Code Webセッション全体を指す。
- 管理ファイル作成だけを独立したセッションとして記録しない。
- 本プロジェクトは「1つの明確な目的＝1セッション」で進める。
- 同じ会話セッション内で追加修正・調整・テスト・再修正を行っても、セッション番号は変更しない。
- 同じセッション内で S02、S03 などへ勝手に番号を進めない。


- S01は、管理ファイルの初期作成と、それに続く最初の実作業（同一セッション内）の両方を含む。
- 初期設定完了時点ではセッション終了処理を行わない。
- 管理ファイル（`CLAUDE.md` / `docs/PROJECT_STATUS.md` / `docs/SESSION_HISTORY.md` /
  `docs/NEXT_TASK.md`）の初期作成は、最初のセッションの開始準備に含める。
- 初期設定完了後は、同じセッション内で実作業へ進む。
- 初期設定完了時点では、セッション終了処理（下記）を行わない。
- 初期設定完了時点では、原則としてコミット・Pushを行わない。

### セッション中の追加依頼

- 同じセッション内の追加依頼は、現在のセッションの作業項目として扱う。
- 追加依頼のたびに `docs/SESSION_HISTORY.md` / `docs/NEXT_TASK.md` を更新しない。
- 軽微な修正・試行錯誤・テスト途中ではコミット・Push しない（検証が通ってから行う）。

### コミットとPush

作業がひと区切りついた時点で、**ユーザーの許可を待たずにコミット・Push する**。

> 2026-09-11、越智さんの指示により変更した。旧ルールは「ユーザーが明示的に指示した
> 場合のみ」だったが、確認の往復が増えるため撤廃した。都度の確認は不要。

- Push先は、そのセッションで指定された開発ブランチとする。指定外のブランチへは
  Push しない。
- **検証が通っていない状態・編集途中の状態ではコミットしない。** 納品できる状態に
  なってからコミットする。
- APIキー・認証情報・業務データはコミットしない（`.gitignore` を確認する）。
- プルリクエストの作成は、引き続き**ユーザーの明示的な指示があった場合のみ**行う。
- コミット後は、コミットハッシュと取得用の `git pull` コマンドをユーザーに伝える。
- 追加依頼ごとに `SESSION_HISTORY.md` / `NEXT_TASK.md` を更新しない。
- 追加依頼ごとにコミットやPushを行わない。
- 軽微な修正、試行錯誤、テスト途中ではコミットやPushを行わない。
- 追加依頼ごとにコミット・Pushを行わない。
- 追加依頼のたびに `SESSION_HISTORY.md` / `NEXT_TASK.md` を更新しない。
- 追加依頼のたびにコミット・Pushを行わない。
- 軽微な修正・試行錯誤・テスト途中ではコミット・Pushを行わない。

### セッション終了処理

セッション終了処理は、ユーザーが以下を明示した場合だけ実施する。

- 「このセッションを終了する」
- 「引継ぎを作成する」
- 「次のセッションへ移る」
- 「セッション終了処理を行う」

個々の作業が完了しただけでは終了処理を開始しない。終了時には以下をまとめて
1回だけ行う。

1. `docs/PROJECT_STATUS.md` を必要に応じて更新する
2. `docs/SESSION_HISTORY.md` に今回のセッションを1件だけ記録する
3. `docs/NEXT_TASK.md` を次回セッション用に更新する
4. 差分とテスト結果を確認する
5. 必要な変更をまとめてコミットする
6. リモートブランチへ Push する
7. 未コミット変更と未追跡ファイルを確認する
8. 次回のセッション番号・タイトル・開始プロンプトを提示する

## 変更してはいけないこと

- 初期化・環境確認だけでセッションを終了しない
- 初期化だけを独立したセッションとして登録しない
- 同じセッション内でセッション番号を増やさない
- 検証が通っていない状態・編集途中の状態でコミット・Push しない
- 依頼範囲外のファイル・ロジックを無関係に変更しない（「ついでに直す」の禁止）
- APIキー・パスワード・認証情報・実際の業務データ（会議議事録・サイトID等）を
  コミットしない。各ツールの `config.json` / トークンキャッシュ / 実行時生成物は
  `.gitignore` で除外し、コミット対象は `config.example.json` などのテンプレート
  のみとする
- ユーザーの指示なしにマージ・リベース・リセットしない

## コード変更時の規約（越智さん向けプロジェクト共通）

- **推測で進めない**：不明点は「不明」「未確認」と明記して質問する
- **変更の最小化**：許可された関数/ファイル以外は変更しない（「ついでに直す」禁止）
- **完全版コード必須**：中略・省略禁止。100行以上の変更は行数差分を明示
- 各ツールフォルダの `CHANGELOG.md` に変更内容を追記する
- バージョン形式：`YYYYMMDD_連番`（例: `20260706_01`）をファイル名に埋め込む
個々の作業が完了しただけでは、終了処理を開始しない。

終了時には以下をまとめて1回だけ行う。

3. `docs/NEXT_TASK.md` を次のセッション用に更新する
6. リモートブランチへPushする
8. 次回のセッション番号、タイトル、開始プロンプトを提示する

### コミットとPush

コミットまたはPushは、以下の場合に限る。
セッション終了処理は、ユーザーが以下のいずれかを明示した場合だけ実施する。

- このセッションを終了する
- 引継ぎを作成する
- 次のセッションへ移る
- セッション終了処理を行う

個々の作業が完了しただけでは終了処理を開始しない。終了時には以下をまとめて1回だけ行う。

1. `PROJECT_STATUS.md` を必要に応じて更新する
2. `SESSION_HISTORY.md` に今回のセッションを1件だけ記録する
3. `NEXT_TASK.md` を次回セッション用に更新する

3. `NEXT_TASK.md` を次のセッション用に更新する
4. 差分とテスト結果を確認する
5. 必要な変更をまとめてコミットする
6. リモートブランチへPushする
7. 未コミット変更・未追跡ファイルを確認する
8. 次回のセッション番号・タイトル・開始プロンプトを提示する

### コミットとPush

コミット・Pushは以下の場合に限る。

- ユーザーが明示的に指示した場合
- チェックポイント保存をユーザーが指示した場合
- セッション終了処理を行う場合

初期設定完了直後や、セッション途中の作業完了時点では、コミット・Pushを行わない。
実作業と管理ファイル更新は、セッション終了時にまとめて保存する。


- 初期化だけでセッションを終了しない
- 細かな作業ごとにコミット・Pushしない
- 既存コード（他ツールフォルダ）を無関係に変更しない
- APIキー、パスワード、認証情報をコミットしない
- ユーザーの指示なしにマージ、リベース、リセットしない
初期設定完了直後や、実作業の途中経過ではコミット・Pushを行わない。

### 機密データの取り扱い（本プロジェクト固有）

対象PDFは社内の実メール（Weekly Report）であり、個人名・メールアドレス・電話番号・
社内プロジェクトコードネームなどを含む。以下を徹底する。

- 元PDFおよびその全文テキストをリポジトリへコミットしない。
- テスト・デバッグ用のPDFは実データではなく、合成（人工生成）した最小サンプルPDFを用いる。
- ドキュメント（IMPLEMENTATION_PLAN.md等）に実データの個人名・メールアドレス・電話番号を
  転記しない。構造・座標・フォント等の技術的知見のみを記録する。

---

## 開発ルール（バージョン管理）※リポジトリ全体の既存ルール

- **ファイル命名**: プログラムを更新する際は、ファイル名を `ツール名_yyyymmdd_連番.py`
  （例: `weekly_pdf_diff_20260729_01.py`）とする。同日に複数回更新する場合は連番を上げる。
- **旧バージョンの保持**: バージョンアップ時に旧ファイルは削除・上書きしない。新旧のファイルを
  フォルダ内に併存させる。
- **CHANGELOG**: 各ツールフォルダに `CHANGELOG.md` を置き、バージョンごとの変更点を記録する。
初期設定完了直後にはコミット・Pushを行わない。実作業と管理ファイル更新は、
セッション終了時にまとめて保存する。

## 変更してはいけないこと

- 初期化だけを独立したセッションとして登録しない
- 同じセッション内でセッション番号を増やさない
- 既存コードを無関係に変更しない
- APIキー・パスワード・認証情報をコミットしない
- ユーザーの指示なしにマージ・リベース・リセットしない
