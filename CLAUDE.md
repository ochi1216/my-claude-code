# CLAUDE.md

> ## ⚠️ RSS オーガナイザー開発は移管済み
>
> 2026-08-13 に `ochi1216/home-pc-workspace` の `rss-organizer/` へ移管した。
> **以後の開発は移管先で行う。このリポジトリの `rss_organizer/` と `docs/` は
> 移管時点の記録であり、更新しない。**
> 以下のセッション管理ルールも、このリポジトリでは実質的に役目を終えている。
> 移管先には同等の `CLAUDE.md` を置いてある。

このリポジトリで Claude Code Web セッションを使って開発する際のセッション管理ルール。
このルールは「RSS オーガナイザー開発」プロジェクト（`rss_organizer/`、管理ファイルは `docs/PROJECT_STATUS.md` / `docs/SESSION_HISTORY.md` / `docs/NEXT_TASK.md`）に適用する。

リポジトリには他にも独立したツール群（`rtocs_organizer/`, `youtube_summary_list_*.py`, `po_database_organizer/`, `shareflex_dashboard/`）があり、それぞれ従来通り `README.md` / `CHANGELOG.md` / `HANDOVER_*.md` で管理する。このセッション管理ルールはそれらには適用しない。

## 開発ルール（バージョン管理・共通）

- **ファイル命名**: プログラムを更新する際は、ファイル名を `ツール名_yyyymmdd_連番.py`（例: `rss_organizer_20260801_01.py`）とする。同日に複数回更新する場合は連番（`_01`, `_02`...）を上げる。
- **旧バージョンの保持**: バージョンアップ時に旧ファイルは削除・上書きしない。新旧のファイルをフォルダ内に併存させる。
- **CHANGELOG**: `rss_organizer/CHANGELOG.md` に、バージョンごとの変更点を記録する。

## セッションの定義

- 1セッションとは、Claude Code Web上で新しく作成された1つの会話セッションを指す。
- このプロジェクトは「1つの明確な目的＝1セッション」で進める。
- セッション番号は、新しいClaude Code Webセッションが作成された場合だけ増やす。
- 同じ会話セッション内で追加修正、調整、テスト、再修正を行っても、セッション番号を変更しない。
- 同じセッション内でS02、S03などへ勝手に番号を進めない。

## 初回セッションの扱い

- 最初のClaude Code WebセッションはS01として扱う（管理ファイルの初期作成＋当該セッションの実作業を含む）。
- 管理ファイル作成だけを独立したセッションとして記録しない。
- 初期設定完了後は、同じセッション内で実作業へ進む。
- 初期設定完了時点では、セッション終了処理・コミット・Pushを行わない。

## セッション中の追加依頼

- 同じセッション内の追加依頼は、現在のセッションの作業項目として扱う。
- 追加依頼ごとに `SESSION_HISTORY.md` / `NEXT_TASK.md` を更新しない。
- 追加依頼ごとにコミットやPushを行わない。
- 軽微な修正、試行錯誤、テスト途中ではコミットやPushを行わない。

## セッション終了処理

セッション終了処理は、ユーザーが以下を明示した場合だけ実施する。

- 「このセッションを終了する」
- 「引継ぎを作成する」
- 「次のセッションへ移る」
- 「セッション終了処理を行う」

個々の作業が完了しただけでは、終了処理を開始しない。

終了時には以下をまとめて1回だけ行う。

1. `docs/PROJECT_STATUS.md` を必要に応じて更新する
2. `docs/SESSION_HISTORY.md` に今回のセッションを1件だけ記録する
3. `docs/NEXT_TASK.md` を次のセッション用に更新する
4. 差分とテスト結果を確認する
5. 必要な変更をまとめてコミットする
6. リモートブランチへPushする
7. 未コミット変更と未追跡ファイルを確認する
8. 次回のセッション番号、タイトル、開始プロンプトを提示する

## コミットとPush

コミットまたはPushは、以下の場合に限る。

- ユーザーが明示的に指示した場合
- チェックポイント保存をユーザーが指示した場合
- セッション終了処理を行う場合

初期設定完了直後には、コミット・Pushを行わない。実作業と管理ファイル更新は、セッション終了時にまとめて保存する。
