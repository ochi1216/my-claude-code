VERSION 20260401.01
追加・修正
自動実行（サイレント）モードの追加: Windowsタスクスケジューラやバッチファイルからの自動実行に対応するため、起動引数 --auto を検知して動作を変えるサイレントモードを実装しました。UIスキップとデフォルト条件の適用: --auto 起動時はGUI（Tkinter）を表示せず、「全カテゴリ対象」「新着のみ抽出」「出版年月フィルタなし」のデフォルト条件を自動適用してバックグラウンド処理へ移行します。ブラウザ起動の抑制と安全な終了: --auto 起動時は処理完了後のブラウザ自動起動（webbrowser.open）をスキップし、無限ループを1回で抜けて（break）プロセスを正常終了させることで、タスクのハングアップを完全に防止しました。

変更関数
main (引数判定、UIスキップ分岐、抽出条件の自動セット、終了処理の分岐追加)

新規追加：

VERSION 20260331.01
追加・修正
パーソナライズ読書ダッシュボード化（キープ機能の実装）:

HTMLサマリー表の「分類」と「書籍名」の間に新しく「キープ」列（チェックボックス）を追加しました。

画面上部に「全チェック」「全チェック解除」「チェックのみ表示」の3つのボタンを追加しました。一括操作は「現在フィルタリングされて表示されている行のみ」に安全に適用されます。

テキスト検索と「チェックのみ表示（トグル）」のAND条件による強力な絞り込みを実現しました。また、既存の「全フィルタ解除」ボタンでこれらの状態も一括リセットされるよう統合しました。

チェックされた行は視認性を高めるために薄い黄色でハイライトされます。

状態の永続化: ユーザーがチェックした本（タイトルと作者で識別）はブラウザのLocalStorageに自動保存され、次回別のサマリーを出力して開いた際にも、過去にキープした本であれば自動的にチェックとハイライトが復元されます。

変更関数
generate_responsive_html (CSSスタイル追加、JavaScriptのLocalStorage制御およびフィルタロジックの拡張、HTML構造へのボタンとキープ列の追加)

新規追加：
なし

VERSION 20260326.01
追加・修正
要約検索の対象サイト拡張: サマリーHTMLの「検索」リンクをクリックした際に実行されるGoogleのサイト指定検索（site: コマンド）の対象ドメインに、「読書メーター（bookmeter.com）」を追加しました。これにより、小説やエッセイ、マイナーな書籍に対する感想・レビューのヒット率とカバレッジが劇的に向上します。

変更関数
generate_responsive_html (検索クエリ文字列への site:bookmeter.com の追加)

新規追加：
なし

VERSION 20260319.05
追加・修正
UI一括トグルボタンの追加: UI画面の最上部に、すべてのチェックボックス（分類カテゴリ＋新着のみ抽出）を一括でON/OFFできるトグルボタンを追加しました。起動時の状態に合わせて初期テキストは「すべて選択解除」とし、1クリックで全解除・全選択を瞬時に切り替えることが可能になり、操作の手間が極限まで削減されました。

変更関数
select_categories_ui (トグルボタンの追加、トグル制御関数の追加、ウィンドウ高さの微調整)

新規追加：
なし

VERSION 20260319.04
追加・修正
UIへの「新着」列の追加: サイト上の総冊数（最新・差分）とは別に、純粋に新しく追加された本が何冊あったかを明示する「新着」列をUIのGrid表に追加しました。
本日の累計新着数の記録ロジック実装: JSONデータベースを拡張し、各カテゴリで新たに見つかった新着本の数を記録する new_books_count を追加しました。同日中に複数回（2周目、3周目）抽出を行った場合でも、本日1日を通じて見つかった累計の新着件数がUIに表示され続けるように状態管理を強化しました。

変更関数
select_categories_ui (UIウィンドウ幅の微調整、「新着」ヘッダーとデータの列追加)
main (JSON構造への new_books_count 追加マイグレーション、抽出時の新着本カウント処理、本日累計または日またぎリセットの保存ロジック追加)

新規追加：
なし

VERSION 20260319.03
追加・修正
UIレイアウト起動エラーの修正: Tkinterの grid 配置において、仕様外のパラメータである sticky="center" を指定していたことにより起動時にクラッシュする問題（_tkinter.TclError）を修正しました。該当のパラメータを削除し、デフォルトの中央揃え動作に任せることで、プログラムが正常に起動し、意図した通りの美しい表形式で表示されるようになります。

変更関数
select_categories_ui (無効な sticky="center" の記述をすべて削除)

新規追加：
なし

VERSION 20260319.02
追加・修正
UIの表形式（Gridレイアウト）化: カテゴリごとの抽出状況を視覚的にわかりやすくするため、UIのレイアウトをTkinterの Grid に変更し、「分類」「前回」「最新」「差分」のヘッダーを持つExcelのような美しい表形式UIへと刷新しました。文字数に関わらず縦列が完璧に揃います。
JSONデータの強制マイグレーション（新構造化）: 既存の count しか持たないデータ構造を、prev_count（前回の冊数）、latest_count（最新の冊数）、last_update_date（最終更新日）を持つ高度な構造へと自動的に移行します。過去の履歴は安全に保護されます。
日付ベースの厳格な状態管理とバグ修正: 起動直後に無条件でJSONを上書きしてしまうバグを完全に削除しました。また、「本日（今日）抽出を完了したカテゴリ」にのみ最新数と差分を表示し、未更新のカテゴリは厳格に「-」と表示して更新漏れを明示するプロフェッショナルな状態管理システムを実装しました。

変更関数
select_categories_ui (Gridレイアウトへの書き換え、日付判定による表示切り替えロジックの追加)
main (JSON構造のマイグレーション処理、無条件上書きの削除、抽出成功後の日付スライド更新処理の追加)

新規追加：
なし

VERSION 20260319.01
追加・修正
UIのデフォルト状態を「全選択」に変更: 毎回の抽出操作におけるクリックの手間を省くため、起動時のUIにおいて「新着のみ抽出」チェックボックスおよび「すべての分類」のチェックボックスがデフォルトでON（チェック済み）の状態で立ち上がるように仕様を変更しました。

変更関数
select_categories_ui (tk.BooleanVar の初期値 value=False を value=True に変更)

新規追加：
なし

VERSION 20260307.04
追加・修正
良質ドメイン串刺し要約検索機能の追加: サマリーHTMLの表の右端に「検索」列を新設し、各書籍に対する「🔍 検索」リンクを配置しました。このリンクにはGoogleの高度な検索コマンド（site:指定）が組み込まれており、クリックするだけでアフィリエイトスパムを排除し、「note」「Qiita」「YouTube」「flier」などの優良な要約・実践レビューサイトのみを即座に串刺し検索できます。
検索精度の最適化: 検索クエリには「書籍名」だけでなく、同名書籍の誤ヒットを防ぐために「作者名（不明以外の場合）」も自動的に結合して送信するロジックを実装しました。さらに、文字化けを防ぐためURLエンコード（urllib.parse.quote）処理を適用しています。

変更関数
グローバル領域 (import urllib.parseの追加)
generate_responsive_html (ヘッダー列の追加、検索クエリの構築、URLエンコード、リンク列の追加)

新規追加：
なし

VERSION 20260307.03
追加・修正
不可視文字（改行・連続空白）の完全サニタイズ: 抽出した「書籍名」「作者名」「出版社名」「出版年月」の文字列に対し、内部に潜む改行コード（\n）やタブ、連続した全角/半角スペースを、すべて「1つの半角スペース」に平準化し、両端の不要な空白を削除（strip）する正規表現クレンジング（re.sub(r'\s+', ' ', text).strip()）を実装しました。
検索バグの根本解消: これにより、「るるぶ大分別府 '27」のように単語間にスペースが含まれるタイトルや、改行が混ざったデータでも、ブラウザの手動入力と全く同じ「綺麗な文字列」として裏側のPOSTフォーム（hiddenタグ）にセットされるようになり、サマリーからの直接検索が失敗する問題が完全に解消されます。見た目のHTML表示およびJSONデータベースへの記録もこの綺麗な文字列で統一されます。

変更関数
fetch_books_from_category (抽出直後の文字列に対する正規表現置換の追加)

新規追加：
なし

VERSION 20260307.02
追加・修正
「新着のみ抽出」機能とUIの追加: 分類選択画面の最上部に「新着のみ抽出（過去に取得した本を除外）」のチェックボックスを追加しました（デフォルトOFF）。これにより、ユーザーは全件抽出か差分抽出かを任意に選択できます。
JSONデータベースの透過的アップグレードと完全差分照合: 既存の category_history.json を拡張し、各カテゴリの「過去に取得した本のリスト（書籍名＋作者）」を記憶できるようにしました。プログラム起動時に古い件数データは壊さずに新形式へ自動移行（マイグレーション）します。
ライフサイクル同期（自動パージ）機能の実装: スクリプトがエラーなくカテゴリの全ページを取得完了した直後に、JSON側の記録を「今回サイトに存在した最新の全件リスト」で丸ごと上書きします。これにより、2週間が経過してサイトから消えた本はJSONからも自動的にパージ（削減）され、データの陳腐化と肥大化を恒久的に防ぎます。
0件時の「空サマリー表示」仕様の適用: 新着が存在しなかった場合や、フィルタにより全件除外された場合でも、プログラムをエラー終了させず、「ヘッダーのみの空HTML」を生成してブラウザで開くように仕様を変更しました。

変更関数
select_categories_ui（「新着のみ抽出」チェックボックスの追加、JSON新構造対応の履歴差分表示、戻り値の拡張）
main（JSONフォーマットのマイグレーション処理、新着差分フィルタリング、JSONの自動パージ上書き保存、0件時のHTML出力処理）

新規追加：
なし

VERSION 20260307.01
追加・修正
出版年月によるフィルタリング機能（UI）の追加: 分類選択UIの「抽出開始」ボタン直上に、開始年月と終了年月（yyyy.m 形式）を指定できる入力欄を追加しました。これにより、特定の期間に出版された新着本だけをピンポイントで抽出することが可能になりました。
堅牢な入力バリデーション: ユーザーの誤入力を防ぐため、フォーマット違反（例：2026年1月）や論理的矛盾（開始年月が終了年月より未来）がある場合は、警告ポップアップを表示して抽出をブロックする仕様を実装しました。
「不明」データの動的処理: 条件が未指定（空欄）の場合は従来通り「不明」データもすべて含めて抽出しますが、年月条件が1つでも指定された場合は、比較不能な「不明」データを自動的に除外（捨てる）する厳格なフィルタリングロジックをPython内部に組み込みました。

変更関数
グローバル領域（tkinter.messagebox のインポート追加）
select_categories_ui（フィルタUI要素の追加、入力値のバリデーション、戻り値の拡張）
main（UIからの条件受け取り、書籍データ群へのフィルタリング適用ロジック追加）

新規追加：
なし

VERSION 20260305.15
追加・修正
リスト構造に対応した冊数抽出ロジックの刷新: 開発者ツールの画像証拠（image_82917e.jpg）に基づき、図書カテゴリ一覧が ol.list-tag 内の li 要素で構成されていることを特定しました。これに合わせ、div.tag-name からカテゴリ名を、div.info p.num.bold から冊数を抽出する高精度なロジックへ変更し、UIへの冊数表示および差分検知を確実にしました。
抽出上限の大幅拡張（案B採用）: 「日本の文学（462冊）」などの大規模カテゴリを網羅するため、1カテゴリあたりの最大取得ページ数を 10ページから30ページ（最大約600冊対応）へ引き上げました。
履歴保存ファイルの配置変更: category_history.json の保存先を、出力フォルダ（Summary）からスクリプトファイルが存在するディレクトリへ変更しました。これによりプログラム本体と管理データのポータビリティを向上させました。

変更関数
グローバル領域（定数 MAX_PAGES_PER_CATEGORY の更新、HISTORY_FILE 定義の修正）
fetch_category_links（HTMLリスト構造に対応した抽出処理への全面書き換え）
main（履歴ファイルパスの決定ロジック修正）

新規追加：
なし

VERSION 20260305.13
追加・修正
UIレイアウトの正常化: 分類選択画面において、右側に浮いてしまっていた「抽出開始」ボタンをウィンドウ最下部に固定配置するよう修正しました。
パッキング順序の最適化: Tkinterの配置ルールに基づき、まず最下部のボタンエリアを確保してから、残りの上部スペースをスクロール可能なリストが占有するよう構成を変更し、画面サイズ変更時もレイアウトが崩れないようにしました。

変更関数
select_categories_ui (配置順序と pack オプションの修正)

新規追加：
なし

VERSION 20260305.12
追加・修正
グローバル定数の完全復旧（NameErrorの解消）: TOP_URL や BASE_URL が定義されていないというエラーを修正しました。パッチ適用時に消失した可能性のある「定数・設定値」セクションをすべて再定義し、プログラム全体で参照可能にしました。
パス設定の一元管理: OUTPUT_DIR と HISTORY_FILE の定義を整理し、ユーザー環境に合わせて一箇所で変更できるように構成しました。

変更関数
グローバル領域（インポート直後の定数定義セクション全体）

新規追加：
なし

VERSION 20260305.11
追加・修正
未定義変数エラー（NameError）の修正: main 関数および他の関数で使用する履歴保存用ファイルのパス定数 HISTORY_FILE を、スクリプト上部のグローバル定義エリアに明示的に配置し、スコープ問題を解消しました。
パス構成の整理: HISTORY_FILE の生成を OUTPUT_DIR に依存させる形で定義し、設定変更を一箇所で管理できるように整理しました。

変更関数
グローバル領域（定数定義セクション）

新規追加：
なし

VERSION 20260305.10
追加・修正
分類別登録件数の可視化: 起動時の分類一覧取得時に、各カテゴリの右側に表示されている「登録冊数（例：49冊）」をスクレイピング対象に追加しました。取得した件数は選択UIのチェックボックス横に表示されます。
履歴管理システム（JSON）の導入: 実行ファイルと同じフォルダに category_history.json を生成し、前回の起動時の冊数を自動的に記録・保存する機能を実装しました。
差分検知（増減表示）機能: 現在の冊数と前回の記録を比較し、増減がある場合にのみ [+10冊増加] や [-5冊減少] といった補助テキストをUIに赤字（または強調）表示する仕組みを追加しました。これにより、データベースが更新されたことを視認できます。
数値抽出の堅牢化: サイト上の「58冊」といった文字列から正規表現を用いて数値のみを抽出し、正確な比較計算（整数演算）を行うロジックを導入しました。

変更関数
グローバル領域 (インポート json の追加)
fetch_category_links (冊数テキストの抽出ロジック追加)
select_categories_ui (履歴比較とラベルへの差分テキスト追加)
main (JSONファイルの読み込み・保存処理の追加)

新規追加：
なし

VERSION 20260305.09
追加・修正
数値ベースのページ逆走・循環防止ロジックの実装: ログの分析結果（page=6 の次にURL形式の異なる page=1 が出現し重複抽出が発生した証拠）に基づき、単純なURL一致チェックを強化しました。正規表現を用いてURLからページ番号を抽出し、「次のページ番号 ≦ 現在のページ番号」となった場合に、URLの表記揺れ（key=... と page=1）を問わず確実にループを停止させるガードを実装しました。「1」と「10」の誤認防止: 文字列の部分一致ではなく、正規表現による数値抽出と整数比較を行うことで、page=10 を page=1 と誤認して途中で止まるリスクを完全に排除しました。正規表現ライブラリの導入: ページ番号の精密な抽出のため、標準ライブラリ re をインポートに追加しました。
変更関数
グローバル領域 (インポート re の追加)
fetch_books_from_category (数値比較による逆走検知ロジックへの刷新)

VERSION 20260305.08
追加・修正
作者・出版社列への自動検索リンク機能の実装: 書籍名と同様に、作者名および出版社名をクリックすることで図書館OPACの「かんたん検索」を実行できる機能を実装しました。これにより、特定の著者や出版社の新着本を即座に二次検索することが可能になります。
作者名の自動サニタイズ（クレンジング）: 検索精度の向上のため、作者名に含まれる「／著」「／監修」などの付加情報を自動的に検出し、区切り文字（／）以降を削除して名前のみを検索ワードとして送信するロジックを追加しました。
出版社検索の最適化: 出版社名については、OPACの部分一致検索特性を活かし、名称をそのまま検索ワードとして使用します。
「不明」データの保護: 著者または出版社が「不明」となっている場合は、検索リンクを生成せず通常のテキストとして表示し、無効な検索が発生しないようガードをかけました。

変更関数
generate_responsive_html (著者・出版社セルへの検索フォーム構造の追加、文字列クレンジング処理の追加)

新規追加：
なし

VERSION 20260305.07
追加・修正
ページ巡回ループ（無限ループ）防止ロジックの実装: 提供されたログ（証拠：page=6 の次に page=1 が出現）に基づき、図書館OPACシステムが最終ページの「次へ」ボタンで先頭ページに戻る挙動、あるいはナビゲーションリンクを誤検知していると判断しました。これを解決するため、抽出中の中間処理に「訪問済みURLチェック（visited_urls）」を導入し、一度訪れたURLに再度遷移しようとした場合は即座にループを終了するガードを実装しました。
抽出精度の適正化: 巡回を検知した時点でループを抜けるため、データが重複して抽出される不具合が解消されます。

変更関数
fetch_books_from_category (訪問済みURLを記録する変数の追加、重複遷移の検知ロジック追加)

新規追加：
なし

VERSION 20260305.06
追加・修正
永続ループ（常駐型）モードの実装: 1回の抽出でプログラムが終了するのではなく、UI（分類選択画面）を閉じない限り、何度でも連続して抽出を繰り返せる while ループ構造に main 関数を改修しました。
ファイル名へのタイムスタンプ追加: 連続実行時にファイルが上書きされるのを防ぐため、出力ファイル名に時分秒（_HHMMSS）を追加しました。これにより、実行のたびにユニークなファイルが生成されます。
UIリセット仕様の適用: 抽出完了後に自動で再表示されるUIのチェックボックスは、要件に基づき毎回すべてOFFの状態で初期化されます。
処理効率の最適化: サーバーへの負荷軽減と速度向上のため、分類一覧（categories）の取得とセッションの確立は起動時の1回のみ行い、ループ内で再利用する設計としました。

変更関数
main (永続ループ構造への書き換え、タイムスタンプ付きファイル名生成、ブラウザ起動後のループ継続処理)

新規追加：
なし

VERSION 20260305.05
追加・修正
HTMLサマリーの自動オープン機能の実装: 利便性向上のため、全ての処理が完了した直後に、生成されたHTMLファイルをOS標準のWebブラウザで自動的に起動する処理を追加しました。
標準ライブラリ webbrowser の導入: プラットフォーム（Windows/Mac/iOS等）を問わず、安全にローカルファイルを開くために Python の標準ライブラリ webbrowser を採用しました。

変更関数
グローバル領域 (インポート追加)
main (処理の最終ステップに webbrowser.open を追加)

新規追加：
なし

VERSION 20260305.04
追加・修正
マルチカラム・リアルタイムフィルタリングの追加: テーブルの各列（分類、書籍名、作者、出版社、出版年月）に対して個別の検索窓（<input type="text">）を設け、文字入力時に即座に該当しない行を非表示にするインクリメンタル・サーチ機能を実装しました。検索はAND条件で動作します。
検索精度の向上: JavaScriptの normalize('NFKC') および toLowerCase() を用いることで、全角/半角、大文字/小文字を区別しない「あいまい検索」を実現しました。
全フィルタ解除ボタンの追加: 表の直上に「全フィルタ解除」ボタンを配置し、ワンクリックで全ての検索窓を空にし、全行を再表示する機能を実装しました。
ソート機能との干渉回避: 検索行（2行目）をクリックした際にソートが誤作動しないよう、ソートのクリックイベントリスナーの対象を <thead> の1行目の <th> のみに限定するようセレクタを修正しました。

変更関数
generate_responsive_html (CSSの追加、検索行・解除ボタンの追加、フィルタリング用JavaScriptの追加、既存JSの修正)

新規追加：
なし

VERSION 20260305.03
追加・修正
テーブルソート機能（JavaScript）の追加: ユーザーのブラウザ上でHTMLサマリの各列を動的に並び替えられるよう、クライアントサイド・ソート機能を追加しました。<th>タグにクリックイベントを付与し、昇順・降順の切り替えおよび視覚的なインジケーター（▲/▼）の表示を実現しています。
出版年月専用のソートロジックの実装: 要件定義に基づき、出版年月の列（インデックス4）に対して特殊なパース処理を実装しました。「不明」は数値の 0 として扱い（昇順で最上部、降順で最下部）、2025.2 や 2025.12 といった文字列は 年 * 100 + 月 の計算式（例：202502, 202512）で整数化して比較することで、完全な時系列ソートを実現しています。

変更関数
generate_responsive_html (CSSの追加、JavaScriptの追加、<table>タグへのID付与)

新規追加：
なし

VERSION 20260305.02
追加・修正
取得上限の撤廃とページネーションの完全実装: 1分類1件の制限を撤廃し、証拠画像（image_9021a4.jpg）から解析したページネーション構造（<li class="next"> 内の <a> タグ）を利用して、次ページが存在する限り自動で遷移し、全件を抽出する while ループ処理を実装しました。
抽出ページURLの動的保持: ユーザー体験向上のため（A案）、抽出した書籍のリンク先（link）が常に「1ページ目のURL」ではなく、「その書籍が実際に表示されていたページ（2ページ目等）のURL」となるように、ループ内で動的に更新される current_url を格納するように変更しました。
フェイルセーフ（無限ループ防止）の導入: 取得上限の代わりに、想定外のシステム挙動による無限ページめくり（サーバーへの過負荷）を防ぐため、最大取得ページ数を制限する定数 MAX_PAGES_PER_CATEGORY（デフォルト10ページ = 最大約500件分）を新設しました。

変更関数
グローバル定数定義部 (MAX_ITEMS_PER_CATEGORY を廃止し、MAX_PAGES_PER_CATEGORY を追加)
fetch_books_from_category (whileループによるページ遷移実装と抽出URLの動的更新)

新規追加：
なし

VERSION 20260305.01
追加・修正
抽出対象の分類選択UIの追加: 要件定義（「チェックボックスで対象を絞り込む。デフォルトは全解除。全選択/全解除ボタン付き。×ボタンで安全に終了」）に基づき、標準ライブラリの tkinter を用いたGUIダイアログ関数 select_categories_ui を追加しました。
処理フローへの割り込み実装: main 関数内において、トップページから分類一覧（categories）を取得した直後にUIを呼び出し、ユーザーが選択したカテゴリのみを後続のスクレイピング処理へ渡すように制御フローを変更しました。×ボタン等でキャンセルされた場合は、安全にスクリプトを終了（Exit）します。
依存ライブラリの追加: UI描画のため、標準ライブラリ tkinter のインポートを追加しました。

変更関数
グローバル領域 (インポート追加)
main (UI呼び出し処理とキャンセル時の終了処理の追加)

新規追加：
select_categories_ui (GUI表示とユーザー選択の取得)

VERSION 20260304.08
追加・修正
検索実行トリガーの偽装データ追加: 手動テストで蔵書詳細検索の初期画面（空欄）が表示された事象（証拠：image_da4d40.png）および、トップページ検索フォームの解析結果（証拠：image_da5be5.jpg）に基づき、サーバーに「検索ボタンが押された」と認識させるための必須パラメータ（name="submit_btn_searchEasy", value="検索"）を非表示フォームに追加しました。これにより、寸止めされていた検索処理が実行され、キーワードが入力された検索結果画面が直接表示されるようになります。

変更関数
generate_responsive_html (フォームタグ内に hidden input を1行追加)

新規追加：
なし

VERSION 20260304.07
追加・修正
「蔵書かんたん検索」フォームのインジェクション実装: 図書館OPACシステムのセッション制約を完全に突破するため、トップページからの正規の検索手順をエミュレートする方式へ変更しました。証拠画像（image_da5be5.jpg）のHTMLソースから解析した「送信先URL（search-standard.do?lang=ja）」「メソッド（POST）」「必須パラメータ（txt_word, hid_word_column）」に基づき、サマリの書籍名セル内に非表示の <form> を動的に生成し、リンククリックによって別タブで検索結果をダイレクトに表示させる仕様を実装しました。
タイトル文字列の安全性向上: 検索フォームの value 属性に書籍名を挿入する際、書籍名にダブルクォーテーション等が含まれていた場合にHTML構造が破綻するのを防ぐため、エスケープ処理（.replace('"', '&quot;')）を追加しました。

変更関数
generate_responsive_html (フォームタグの追加、リンク構造の変更、エスケープ処理追加)

新規追加：
なし（既存関数の書き換えのみ）

VERSION 20260304.06
追加・修正
リンク先を分類一覧ページ（正門）へ変更: 図書館OPACシステムの強固なセッション制約と現代のブラウザのサードパーティクッキー制限を考慮し、書籍詳細ページへの直接遷移（ハッキングアプローチ）を破棄しました。代わりに、書籍名のリンク先をその本が属する「新着一覧ページ（category_url）」へ直接設定する堅牢な仕様に変更しました。
不要なJavaScriptハックの完全削除: 詳細ページへの強制遷移のために追加していた openBook 関数（JavaScript）や関連する onclick イベントをHTMLから完全に削除し、ピュアで安定したHTML構造へ回帰させました。
不要なリンク抽出ロジックの削除: リンク先が一覧ページに固定されたため、fetch_books_from_category 内で switch-detail.do の相対URLを取得して変数に格納する処理を廃止し、辞書の link キーに直接 category_url を代入するようコードをクリーンアップしました。

変更関数
fetch_books_from_category (抽出URLの置き換え、不要ロジック削除)
generate_responsive_html (JavaScript削除、aタグのhref復元)

新規追加：
なし

VERSION 20260304.05
追加・修正
Named Window Routingによるセッション維持ロジックの導入: 前回の検証結果（iframeによるサードパーティクッキー制限でのセッション破棄）を根拠に、非表示のiframeを廃止しました。代わりに、正規の別タブ（First-Partyコンテキスト）を名前付き（BookTab）で開き、3.5秒待機後に同じ名前のタブに対して詳細URLをリクエストするJavaScript（openBook）へ書き換えました。これにより、ブラウザのセキュリティ制限を回避しつつ、手動検証時と全く同じセッション維持状態をプログラムで再現します。
不要要素の削除: セッション破棄の原因となっていた<body>末尾の非表示iframeを削除しました。

変更関数
generate_responsive_html (JavaScriptロジックの変更とiframeの削除)
※ fetch_books_from_category は前回追加した category_url の保持状態を維持します。

新規追加：
なし（既存関数の書き換えのみ）

VERSION 20260304.04
追加・修正
iframeを用いた並行セッション維持の実装: ユーザーによる手動検証の成功（「一覧ページのタブを開いたまま別タブで詳細URLを開くと成功する」という事実）を根拠とし、HTML内に非表示の iframe を設置して裏で一覧ページをロードさせ、ブラウザにセッション状態を維持させるロジックに変更しました。
ポップアップブロック回避機構の追加: 現代のブラウザでは setTimeout 等の非同期処理内での window.open（新しいタブを開く挙動）が迷惑ポップアップとして強制ブロックされるため、クリックと同時に同期的に空のタブを開いておき、時間差でそのタブのURLを詳細ページ（idx=X）に書き換える安全なJSロジックを採用しました。
カテゴリURLのデータ保持: 各書籍データに親の一覧ページURL（category_url）を保持する処理を追加しました。

変更関数
fetch_books_from_category (出力データへのカテゴリURL追加)
generate_responsive_html (JavaScriptの埋め込み、非表示iframeの追加、<a>タグの書き換え)

新規追加：
なし（既存関数の書き換えのみ）

VERSION 20260304.03
追加・修正
セッション生成用JavaScriptの追加: ユーザーによる「一覧ページを踏んだ後に詳細ページへ遷移すれば開く」という検証結果（証拠）に基づき、HTML出力時に2段階遷移を自動実行するJavaScript関数（openBook）を追加しました。これにより、1.5秒の待機時間でOPACのセッション要件をクリアし、正しい詳細ページを表示させます。
カテゴリURLのデータ保持: 上記の2段階遷移を実現するため、各書籍データに「親となる一覧ページのURL（category_url）」を保持させる修正を行いました。
リンク生成ロジックのUI変更: 書籍名のリンクを直接のURL（href）から、追加したJavaScript関数を呼び出すクリックイベント（onclick）に変更しました。

変更関数
fetch_books_from_category (出力データにカテゴリURLを追加)
generate_responsive_html (JavaScriptの埋め込みと<a>タグの書き換え)

新規追加：
なし（既存関数の書き換えのみ）

VERSION 20260304.02
追加・修正
固有ID（パーマリンク）錬成ロジックの追加: 事前調査の画像群に基づく「OPACシステムの仕様（一覧の li タグ内に予約用チェックボックスが存在する）」を根拠とし、書籍ごとの絶対リンク（パーマリンク）を生成する処理を追加しました。<div class="report"> の親要素である <li> タグから <input type="checkbox"> を探し、その value 属性（固有ID）を抽出して永続的なURL（switch-detail.do?bibid=【固有ID】）を構築・上書きします。

変更関数
fetch_books_from_category (URL変数 link の上書き処理を追加)

新規追加：
なし

VERSION 20260304.01
追加・修正
取得件数の変更: ご要望（「１項目５冊をとってきていましたが、１冊に変更」）に基づき、各分類からの最大取得件数を定義する定数 MAX_ITEMS_PER_CATEGORY を 5 から 1 に変更しました。

変更関数
グローバル定数定義部 (MAX_ITEMS_PER_CATEGORY)

新規追加：
なし

VERSION 2026.0303.04
追加・修正
出版社と出版年月のデータ分離: fetch_books_from_category 内でのテキスト分割処理において、結合して格納していた「出版社・出版年月」を、それぞれ publisher と published_date として独立したキーに分けて格納するように変更しました。HTMLテーブル構造の列構成変更: generate_responsive_html 内のテーブルヘッダー（<th>）およびデータ行（<td>）において、一番右の独立した「リンク」列を削除し、新たに「出版社」と「出版年月」の列を分離して配置しました。書籍名へのリンク統合: generate_responsive_html 内で、書籍名のテキスト自体に <a> タグを適用し、抽出した詳細ページへのリンク（item['link']）を設定しました。

変更関数
fetch_books_from_category (抽出データの変数分離)
generate_responsive_html (HTMLテーブル構成の変更とリンクの統合)

新規追加：
なし

VERSION 2026.0303.04（抽出ロジック再確認版）
追加・修正
抽出ロジックの改善と再確認: 開発者ツールの画像から判別した div.report と switch-detail.do を起点とした構造を引き続き適用しています。書籍の作者や出版社情報を、<p> タグ内の文字列分割処理を用いて抽出する機能を確定させました。

変更関数
fetch_books_from_category

新規追加：
なし

VERSION 2026.0303.03
追加・修正
書籍抽出の起点を div.report に変更: 提供された開発者ツールの画像（証拠）に基づき、書籍1件のデータが <div class="report"> に格納されていることが判明したため、不要なリンク（ヘッダー等の「資料検索」など）を除外でき、かつ正確に書籍ブロックのみをループできる構造に改修しました。
書籍リンクの抽出条件修正: 画像の証拠に基づき、リンク先のURLが search-detail.do ではなく switch-detail.do であることが判明したため、抽出条件を修正しました。これにより取得件数が0件になるBugが解消されます。
著者・出版社・出版年月の抽出ロジック追加: 画像の証拠に基づき、<div class="column info"> 内の <p> タグからテキストを取得し、「--」で分割してメタデータを抽出する処理を実装しました。

変更関数
fetch_books_from_category (抽出ロジックの全面的なDOM構造対応への書き換え)

VERSION 2026.0303.02
追加・修正
「資料検索」誤抽出の回避（ヘッダーメニュー除外）: 提供された画像（出力結果と実際の画面）を根拠とし、書籍名がすべて「資料検索」となる不具合を解消しました。fetch_books_from_category 内にて、取得したリンクのテキストが「資料検索」「資料紹介」等の共通ナビゲーションメニューである場合はスキップする処理を追加しました。
メタデータ抽出の保留: 画像から「--」区切りで著者や出版社が表示されている事実は確認できましたが、対象のHTMLタグ構造が依然として不明なため、推測による実装を回避し「不明」のままとしています（Change Request参照）。

変更関数
fetch_books_from_category (抽出スキップ条件の追加)

新規追加：
なし

VERSION 2026.0303.01（初版）
変更関数
（※新規作成のため既存関数の変更はありません）
新規追加：
setup_directory(): 出力先ディレクトリの存在確認と作成
fetch_category_links(): 新着資料トップから各分類のURLと分類名を取得
fetch_books_from_category(): 各分類の一覧ページから書籍のメタデータを抽出（最大5件）
generate_responsive_html(): 取得データを基にモバイル対応のHTMLファイルを生成
main(): 全体の処理制御およびエラー発生時のフェイルセーフ（継続）処理
